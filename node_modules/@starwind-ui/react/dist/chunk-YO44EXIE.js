import {
  ReactPortal,
  ReactPortalScopeProvider,
  useReactPortalRuntimeLifecycle,
  useReactPortalScope
} from "./chunk-PUUZDX3Q.js";
import {
  getAsChildElement,
  getElementRef,
  mergeAsChildProps,
  setRef,
  useComposedRefs,
  useIsomorphicLayoutEffect
} from "./chunk-I44ESSZ2.js";

// src/tooltip/TooltipArrow.tsx
import * as React from "react";
import { jsx } from "react/jsx-runtime";
var TooltipArrow = React.forwardRef(
  function TooltipArrow2(props, forwardedRef) {
    return /* @__PURE__ */ jsx("div", { "data-sw-tooltip-arrow": true, "data-state": "closed", ref: forwardedRef, ...props });
  }
);
TooltipArrow.displayName = "Tooltip.Arrow";
var TooltipArrow_default = TooltipArrow;

// src/tooltip/TooltipPopup.tsx
import * as React2 from "react";
import { jsx as jsx2 } from "react/jsx-runtime";
var TooltipPopup = React2.forwardRef(function TooltipPopup2({ side = "top", align = "center", sideOffset = 8, avoidCollisions = true, ...props }, forwardedRef) {
  return /* @__PURE__ */ jsx2(
    "div",
    {
      "data-sw-tooltip-popup": true,
      role: "tooltip",
      "data-state": "closed",
      "data-side": side,
      "data-align": align,
      "data-side-offset": sideOffset,
      "data-avoid-collisions": avoidCollisions ? "true" : "false",
      hidden: true,
      ref: forwardedRef,
      ...props
    }
  );
});
TooltipPopup.displayName = "Tooltip.Popup";
var TooltipPopup_default = TooltipPopup;

// src/tooltip/TooltipPortal.tsx
import { reportPortalPlacement, resolvePortalPlacement } from "@starwind-ui/runtime/tooltip";
import * as React3 from "react";
import { jsx as jsx3 } from "react/jsx-runtime";
var TooltipPortal = React3.forwardRef(
  function TooltipPortal2(props, forwardedRef) {
    return /* @__PURE__ */ jsx3(
      ReactPortal,
      {
        discoveryAttribute: "data-sw-tooltip-portal",
        reportPlacement: reportPortalPlacement,
        resolvePlacement: resolvePortalPlacement,
        ref: forwardedRef,
        ...props
      }
    );
  }
);
TooltipPortal.displayName = "Tooltip.Portal";
var TooltipPortal_default = TooltipPortal;

// src/tooltip/TooltipPositioner.tsx
import * as React4 from "react";
import { jsx as jsx4 } from "react/jsx-runtime";
var TooltipPositioner = React4.forwardRef(
  function TooltipPositioner2({ side = "top", align = "center", sideOffset = 8, avoidCollisions = true, ...props }, forwardedRef) {
    return /* @__PURE__ */ jsx4(
      "div",
      {
        "data-sw-tooltip-positioner": true,
        "data-state": "closed",
        "data-side": side,
        "data-align": align,
        "data-side-offset": sideOffset,
        "data-avoid-collisions": avoidCollisions ? "true" : "false",
        ref: forwardedRef,
        ...props
      }
    );
  }
);
TooltipPositioner.displayName = "Tooltip.Positioner";
var TooltipPositioner_default = TooltipPositioner;

// src/tooltip/TooltipRoot.tsx
import {
  createPortalBinding,
  createTooltip,
  refreshTooltipPortalSurface
} from "@starwind-ui/runtime/tooltip";
import * as React5 from "react";
import { jsx as jsx5 } from "react/jsx-runtime";
var TooltipRoot = React5.forwardRef(function TooltipRoot2({
  defaultOpen = false,
  open,
  closeDelay = 200,
  closeOnEscape = true,
  closeOnOutsideInteract = true,
  disabled = false,
  disableHoverableContent = false,
  openDelay = 200,
  onOpenChange,
  ...props
}, forwardedRef) {
  const rootRef = React5.useRef(null);
  const portalScope = useReactPortalScope(rootRef, createPortalBinding);
  const portalRuntimeActivation = portalScope.activation;
  const instanceRef = React5.useRef(void 0);
  const onOpenChangeRef = React5.useRef(onOpenChange);
  const openRef = React5.useRef(open);
  const defaultOpenRef = React5.useRef(defaultOpen);
  const [uncontrolledOpen, setUncontrolledOpenState] = React5.useState(defaultOpenRef.current);
  const uncontrolledOpenRef = React5.useRef(uncontrolledOpen);
  const setUncontrolledOpen = React5.useCallback((nextOpen) => {
    uncontrolledOpenRef.current = nextOpen;
    setUncontrolledOpenState(nextOpen);
  }, []);
  useIsomorphicLayoutEffect(() => {
    onOpenChangeRef.current = onOpenChange;
  }, [onOpenChange]);
  useIsomorphicLayoutEffect(() => {
    openRef.current = open;
  }, [open]);
  const composedRef = React5.useCallback(
    (node) => {
      rootRef.current = node;
      return setRef(forwardedRef, node);
    },
    [forwardedRef]
  );
  const initializePortalRuntime = React5.useCallback(() => {
    const root = rootRef.current;
    if (!root) return;
    const instance = createTooltip(root, {
      defaultOpen: uncontrolledOpenRef.current,
      closeDelay,
      closeOnEscape,
      closeOnOutsideInteract,
      disabled,
      disableHoverableContent,
      openDelay,
      onOpenChange: (nextOpen, details) => {
        onOpenChangeRef.current?.(nextOpen, details);
      },
      ...openRef.current !== void 0 ? { open: openRef.current } : {}
    });
    instanceRef.current = instance;
    const unsubscribeOpenChange = instance.subscribe("openChange", (details) => {
      if (openRef.current === void 0) {
        setUncontrolledOpen(details.open);
      }
    });
    return () => {
      unsubscribeOpenChange();
      instance.destroy();
      if (instanceRef.current === instance) {
        instanceRef.current = void 0;
      }
    };
  }, [closeDelay, closeOnEscape, closeOnOutsideInteract, disableHoverableContent, openDelay]);
  useReactPortalRuntimeLifecycle(portalScope, initializePortalRuntime);
  useIsomorphicLayoutEffect(() => {
    if (!portalScope.isReady()) return;
    const root = rootRef.current;
    if (!root) return;
    refreshTooltipPortalSurface(root);
  }, [portalRuntimeActivation]);
  useIsomorphicLayoutEffect(() => {
    const instance = instanceRef.current;
    if (!instance) return;
    instance.setDisabled(disabled);
    if (disabled && openRef.current === void 0) {
      setUncontrolledOpen(false);
    }
  }, [disabled, setUncontrolledOpen]);
  useIsomorphicLayoutEffect(() => {
    if (open === void 0) return;
    const instance = instanceRef.current;
    if (!instance) return;
    if (instance.getOpen() === open) return;
    instance.setOpen(open, { emit: false });
  }, [disabled, open]);
  const renderedOpen = !disabled && (open ?? uncontrolledOpen);
  return /* @__PURE__ */ jsx5(ReactPortalScopeProvider, { scope: portalScope, children: /* @__PURE__ */ jsx5(
    "div",
    {
      "data-sw-tooltip": true,
      "data-default-open": defaultOpenRef.current ? "true" : void 0,
      "data-close-delay": closeDelay,
      "data-close-on-escape": closeOnEscape ? "true" : "false",
      "data-close-on-outside-interact": closeOnOutsideInteract ? "true" : "false",
      "data-content-hoverable": !disableHoverableContent ? "true" : "false",
      "data-disabled": disabled ? "" : void 0,
      "data-open-delay": openDelay,
      "data-state": renderedOpen ? "open" : "closed",
      ref: composedRef,
      ...props
    }
  ) });
});
TooltipRoot.displayName = "Tooltip.Root";
var TooltipRoot_default = TooltipRoot;

// src/tooltip/TooltipTrigger.tsx
import * as React6 from "react";
import { jsx as jsx6 } from "react/jsx-runtime";
var TooltipTrigger = React6.forwardRef(function TooltipTrigger2({ asChild = false, children, className, disabled = false, ...props }, forwardedRef) {
  const protectedTriggerProps = {
    "data-sw-tooltip-trigger": "",
    "data-disabled": disabled ? "" : void 0,
    "aria-disabled": disabled ? "true" : void 0,
    "data-state": "closed"
  };
  const triggerProps = {
    ...protectedTriggerProps,
    ...props
  };
  const asChildElement = getAsChildElement(children);
  const composedRef = useComposedRefs(
    forwardedRef,
    asChildElement ? getElementRef(asChildElement) : void 0
  );
  if (asChild && asChildElement) {
    const child = asChildElement;
    const childProps = child.props;
    const mergedProps = mergeAsChildProps({ ...triggerProps, className }, childProps, {
      protectedProps: protectedTriggerProps
    });
    return React6.cloneElement(child, {
      ...mergedProps,
      ref: composedRef
    });
  }
  return /* @__PURE__ */ jsx6(
    "button",
    {
      type: "button",
      className,
      disabled,
      ref: forwardedRef,
      ...triggerProps,
      children
    }
  );
});
TooltipTrigger.displayName = "Tooltip.Trigger";
var TooltipTrigger_default = TooltipTrigger;

// src/tooltip/index.ts
var Tooltip = {
  Root: TooltipRoot_default,
  Trigger: TooltipTrigger_default,
  Portal: TooltipPortal_default,
  Positioner: TooltipPositioner_default,
  Popup: TooltipPopup_default,
  Arrow: TooltipArrow_default
};
var tooltip_default = Tooltip;

export {
  TooltipArrow_default,
  TooltipPopup_default,
  TooltipPortal_default,
  TooltipPositioner_default,
  TooltipRoot_default,
  TooltipTrigger_default,
  Tooltip,
  tooltip_default
};
