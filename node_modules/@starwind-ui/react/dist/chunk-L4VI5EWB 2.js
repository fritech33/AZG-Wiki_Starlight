import {
  useClosePresence
} from "./chunk-4ZBBDK5W.js";
import {
  ReactPortal,
  ReactPortalScopeProvider,
  useReactPortalScope
} from "./chunk-PUUZDX3Q.js";
import {
  getAsChildElement,
  getElementRef,
  mergeAsChildProps,
  setRef,
  useComposedRefs,
  useIsomorphicLayoutEffect
} from "./chunk-I44ESSZ2.js";

// src/select/SelectGroup.tsx
import * as React from "react";
import { jsx } from "react/jsx-runtime";
var SelectGroup = React.forwardRef(
  function SelectGroup2(props, forwardedRef) {
    return /* @__PURE__ */ jsx("div", { "data-sw-select-group": true, role: "group", ref: forwardedRef, ...props });
  }
);
SelectGroup.displayName = "Select.Group";
var SelectGroup_default = SelectGroup;

// src/select/SelectGroupLabel.tsx
import * as React2 from "react";
import { jsx as jsx2 } from "react/jsx-runtime";
var SelectGroupLabel = React2.forwardRef(
  function SelectGroupLabel2(props, forwardedRef) {
    return /* @__PURE__ */ jsx2("div", { "data-sw-select-group-label": true, ref: forwardedRef, ...props });
  }
);
SelectGroupLabel.displayName = "Select.GroupLabel";
var SelectGroupLabel_default = SelectGroupLabel;

// src/select/SelectIcon.tsx
import * as React3 from "react";
import { jsx as jsx3 } from "react/jsx-runtime";
var SelectIcon = React3.forwardRef(
  function SelectIcon2(props, forwardedRef) {
    return /* @__PURE__ */ jsx3("span", { "data-sw-select-icon": true, "aria-hidden": "true", ref: forwardedRef, ...props });
  }
);
SelectIcon.displayName = "Select.Icon";
var SelectIcon_default = SelectIcon;

// src/select/SelectItem.tsx
import * as React5 from "react";

// src/select/SelectContext.tsx
import * as React4 from "react";
var fallbackSelectContext = {
  disabled: false,
  open: false,
  readOnly: false,
  required: false,
  value: null,
  selectedLabel: null
};
var fallbackSelectItemContext = {
  value: ""
};
var SelectContext = React4.createContext(null);
var SelectItemContext = React4.createContext(null);
function useSelectContext() {
  return React4.useContext(SelectContext) ?? fallbackSelectContext;
}
function useSelectItemContext() {
  return React4.useContext(SelectItemContext) ?? fallbackSelectItemContext;
}

// src/select/SelectItem.tsx
import { jsx as jsx4 } from "react/jsx-runtime";
var SelectItem = React5.forwardRef(function SelectItem2({ disabled = false, value, ...props }, forwardedRef) {
  const select = useSelectContext();
  const selected = select.value === value;
  const itemContextValue = React5.useMemo(() => ({ value }), [value]);
  return /* @__PURE__ */ jsx4(SelectItemContext.Provider, { value: itemContextValue, children: /* @__PURE__ */ jsx4(
    "div",
    {
      "data-sw-select-item": true,
      "data-value": value,
      role: "option",
      "aria-selected": selected,
      "aria-disabled": disabled || void 0,
      "data-disabled": disabled ? "" : void 0,
      "data-selected": selected ? "" : void 0,
      tabIndex: -1,
      ref: forwardedRef,
      ...props
    }
  ) });
});
SelectItem.displayName = "Select.Item";
var SelectItem_default = SelectItem;

// src/select/SelectItemIndicator.tsx
import * as React6 from "react";
import { jsx as jsx5 } from "react/jsx-runtime";
var SelectItemIndicator = React6.forwardRef(
  function SelectItemIndicator2(props, forwardedRef) {
    const select = useSelectContext();
    const item = useSelectItemContext();
    const selected = select.value === item.value;
    return /* @__PURE__ */ jsx5(
      "span",
      {
        "data-sw-select-item-indicator": true,
        "aria-hidden": "true",
        "data-state": selected ? "checked" : "unchecked",
        "data-visible": selected ? "" : void 0,
        "data-hidden": selected ? void 0 : "",
        hidden: !selected,
        ref: forwardedRef,
        ...props
      }
    );
  }
);
SelectItemIndicator.displayName = "Select.ItemIndicator";
var SelectItemIndicator_default = SelectItemIndicator;

// src/select/SelectItemText.tsx
import * as React7 from "react";
import { jsx as jsx6 } from "react/jsx-runtime";
var SelectItemText = React7.forwardRef(
  function SelectItemText2(props, forwardedRef) {
    return /* @__PURE__ */ jsx6("span", { "data-sw-select-item-text": true, ref: forwardedRef, ...props });
  }
);
SelectItemText.displayName = "Select.ItemText";
var SelectItemText_default = SelectItemText;

// src/select/SelectLabel.tsx
import * as React8 from "react";
import { jsx as jsx7 } from "react/jsx-runtime";
var SelectLabel = React8.forwardRef(
  function SelectLabel2(props, forwardedRef) {
    return /* @__PURE__ */ jsx7("div", { "data-sw-select-label": true, ref: forwardedRef, ...props });
  }
);
SelectLabel.displayName = "Select.Label";
var SelectLabel_default = SelectLabel;

// src/select/SelectList.tsx
import * as React9 from "react";
import { jsx as jsx8 } from "react/jsx-runtime";
var SelectList = React9.forwardRef(
  function SelectList2(props, forwardedRef) {
    return /* @__PURE__ */ jsx8("div", { "data-sw-select-list": true, ref: forwardedRef, ...props });
  }
);
SelectList.displayName = "Select.List";
var SelectList_default = SelectList;

// src/select/SelectPopup.tsx
import * as React10 from "react";
import { jsx as jsx9 } from "react/jsx-runtime";
var SelectPopup = React10.forwardRef(function SelectPopup2({
  align = "start",
  alignOffset = 0,
  avoidCollisions = true,
  keepMounted = false,
  side = "bottom",
  sideOffset = 4,
  ...props
}, forwardedRef) {
  const select = useSelectContext();
  const closePresence = useClosePresence({
    keepMounted,
    open: select.open
  });
  const composedRef = useComposedRefs(forwardedRef, closePresence.ref);
  return /* @__PURE__ */ jsx9(
    "div",
    {
      "data-sw-select-popup": true,
      role: "listbox",
      tabIndex: -1,
      "data-state": select.open ? "open" : "closed",
      "data-side": side,
      "data-align": align,
      "data-side-offset": sideOffset,
      "data-align-offset": alignOffset,
      "data-avoid-collisions": avoidCollisions ? "true" : "false",
      hidden: closePresence.hidden,
      ref: composedRef,
      ...props,
      children: closePresence.present ? props.children : null
    }
  );
});
SelectPopup.displayName = "Select.Popup";
var SelectPopup_default = SelectPopup;

// src/select/SelectPortal.tsx
import { reportPortalPlacement, resolvePortalPlacement } from "@starwind-ui/runtime/select";
import * as React11 from "react";
import { jsx as jsx10 } from "react/jsx-runtime";
var SelectPortal = React11.forwardRef(
  function SelectPortal2(props, forwardedRef) {
    return /* @__PURE__ */ jsx10(
      ReactPortal,
      {
        discoveryAttribute: "data-sw-select-portal",
        reportPlacement: reportPortalPlacement,
        resolvePlacement: resolvePortalPlacement,
        ref: forwardedRef,
        ...props
      }
    );
  }
);
SelectPortal.displayName = "Select.Portal";
var SelectPortal_default = SelectPortal;

// src/select/SelectPositioner.tsx
import * as React12 from "react";
import { jsx as jsx11 } from "react/jsx-runtime";
var SelectPositioner = React12.forwardRef(
  function SelectPositioner2({
    align = "start",
    alignOffset = 0,
    alignItemWithTrigger = true,
    avoidCollisions = true,
    side = "bottom",
    sideOffset = 4,
    ...props
  }, forwardedRef) {
    const select = useSelectContext();
    return /* @__PURE__ */ jsx11(
      "div",
      {
        "data-sw-select-positioner": true,
        "data-state": select.open ? "open" : "closed",
        "data-side": side,
        "data-align": align,
        "data-side-offset": sideOffset,
        "data-align-offset": alignOffset,
        "data-align-item-with-trigger": alignItemWithTrigger ? "true" : "false",
        "data-avoid-collisions": avoidCollisions ? "true" : "false",
        ref: forwardedRef,
        ...props
      }
    );
  }
);
SelectPositioner.displayName = "Select.Positioner";
var SelectPositioner_default = SelectPositioner;

// src/select/SelectRoot.tsx
import {
  createPortalBinding,
  createSelect,
  refreshSelectPortalSurface
} from "@starwind-ui/runtime/select";
import * as React13 from "react";
import { jsx as jsx12, jsxs } from "react/jsx-runtime";
var SelectRoot = React13.forwardRef(function SelectRoot2({
  autoComplete,
  defaultOpen = false,
  defaultValue,
  disabled = false,
  form,
  highlightItemOnHover = true,
  modal = true,
  name,
  onOpenChange,
  onValueChange,
  open,
  readOnly = false,
  required = false,
  value,
  children,
  ...props
}, forwardedRef) {
  const rootRef = React13.useRef(null);
  const portalScope = useReactPortalScope(rootRef, createPortalBinding);
  const portalRuntimeActivation = portalScope.activation;
  const instanceRef = React13.useRef(void 0);
  const onOpenChangeRef = React13.useRef(onOpenChange);
  const onValueChangeRef = React13.useRef(onValueChange);
  const openRef = React13.useRef(open);
  const valueRef = React13.useRef(value);
  const defaultOpenRef = React13.useRef(defaultOpen);
  const defaultValueRef = React13.useRef(defaultValue);
  const [uncontrolledOpen, setUncontrolledOpenState] = React13.useState(defaultOpenRef.current);
  const [uncontrolledValue, setUncontrolledValueState] = React13.useState(defaultValueRef.current);
  const uncontrolledOpenRef = React13.useRef(uncontrolledOpen);
  const uncontrolledValueRef = React13.useRef(uncontrolledValue);
  const pendingProgrammaticValueRef = React13.useRef(null);
  const setUncontrolledOpen = React13.useCallback((nextOpen) => {
    uncontrolledOpenRef.current = nextOpen;
    setUncontrolledOpenState(nextOpen);
  }, []);
  const setUncontrolledValue = React13.useCallback((nextValue) => {
    uncontrolledValueRef.current = nextValue;
    setUncontrolledValueState(nextValue);
  }, []);
  useIsomorphicLayoutEffect(() => {
    onOpenChangeRef.current = onOpenChange;
  }, [onOpenChange]);
  useIsomorphicLayoutEffect(() => {
    onValueChangeRef.current = onValueChange;
  }, [onValueChange]);
  useIsomorphicLayoutEffect(() => {
    openRef.current = open;
  }, [open]);
  useIsomorphicLayoutEffect(() => {
    valueRef.current = value;
  }, [value]);
  const composedRef = React13.useCallback(
    (node) => {
      rootRef.current = node;
      return setRef(forwardedRef, node);
    },
    [forwardedRef]
  );
  const ensureInstance = React13.useCallback(() => {
    const existing = instanceRef.current;
    if (existing) return existing;
    const root = rootRef.current;
    if (!root) return void 0;
    if (!portalScope.isReady()) return;
    const instance = createSelect(root, {
      defaultOpen: uncontrolledOpenRef.current,
      defaultValue: uncontrolledValueRef.current,
      disabled,
      autoComplete,
      form,
      highlightItemOnHover,
      modal,
      onOpenChange: (nextOpen, details) => {
        onOpenChangeRef.current?.(nextOpen, details);
      },
      onValueChange: (nextValue, details) => {
        onValueChangeRef.current?.(nextValue, details);
      },
      name,
      readOnly,
      required,
      ...openRef.current !== void 0 ? { open: openRef.current } : {},
      ...valueRef.current !== void 0 ? { value: valueRef.current } : {}
    });
    instanceRef.current = instance;
    instance.subscribe("openChange", (details) => {
      if (openRef.current === void 0) {
        setUncontrolledOpen(details.open);
      }
    });
    instance.subscribe("valueChange", (details) => {
      const nextSelectedLabel = getTextFromSelectItem(details.item);
      if (nextSelectedLabel !== null || details.value === null) {
        setSelectedLabel({
          label: nextSelectedLabel,
          value: details.value
        });
      }
      if (valueRef.current === void 0) {
        setUncontrolledValue(details.value);
      }
    });
    return instance;
  }, [
    autoComplete,
    disabled,
    form,
    highlightItemOnHover,
    modal,
    name,
    readOnly,
    required,
    setUncontrolledOpen,
    setUncontrolledValue,
    portalRuntimeActivation
  ]);
  useIsomorphicLayoutEffect(() => {
    if (!portalScope.isReady()) return;
    const root = rootRef.current;
    if (!root) return;
    refreshSelectPortalSurface(root);
  }, [portalRuntimeActivation]);
  useIsomorphicLayoutEffect(() => {
    const root = rootRef.current;
    if (!root) return;
    const handleProgrammaticValueCommand = (event) => {
      if (!(event instanceof CustomEvent) || !event.detail || typeof event.detail !== "object")
        return;
      const detail = event.detail;
      if (!Object.hasOwn(detail, "value")) return;
      if (detail.value !== null && typeof detail.value !== "string") return;
      event.stopImmediatePropagation();
      const nextValue = detail.value === "" ? null : detail.value;
      const emit = typeof detail.emit === "boolean" ? detail.emit : void 0;
      const instance = ensureInstance();
      if (!instance) {
        pendingProgrammaticValueRef.current = { emit, value: nextValue };
        if (emit !== false || valueRef.current !== void 0) return;
        setSelectedLabel({
          label: findSelectedOptionText(children, nextValue),
          value: nextValue
        });
        setUncontrolledValue(nextValue);
        return;
      }
      instance.setValue(nextValue, { emit });
      if (emit !== false || valueRef.current !== void 0) return;
      setSelectedLabel({
        label: findSelectedOptionText(children, nextValue),
        value: nextValue
      });
      setUncontrolledValue(nextValue);
    };
    root.addEventListener("starwind:set-value", handleProgrammaticValueCommand, {
      capture: true
    });
    return () => {
      root.removeEventListener("starwind:set-value", handleProgrammaticValueCommand, {
        capture: true
      });
    };
  }, [children, ensureInstance, setUncontrolledValue]);
  useIsomorphicLayoutEffect(() => {
    const pending = pendingProgrammaticValueRef.current;
    if (!pending) return;
    const instance = ensureInstance();
    if (!instance) return;
    pendingProgrammaticValueRef.current = null;
    instance.setValue(pending.value, { emit: pending.emit });
  }, [ensureInstance]);
  useIsomorphicLayoutEffect(() => {
    return () => {
      instanceRef.current?.destroy();
      instanceRef.current = void 0;
    };
  }, []);
  useIsomorphicLayoutEffect(() => {
    if ((openRef.current ?? uncontrolledOpenRef.current) !== true) return;
    ensureInstance();
  }, [ensureInstance, open, uncontrolledOpen]);
  useIsomorphicLayoutEffect(() => {
    instanceRef.current?.setFormOptions({ autoComplete, form, name, required });
  }, [autoComplete, form, name, required]);
  useIsomorphicLayoutEffect(() => {
    instanceRef.current?.setDisabled(disabled);
  }, [disabled]);
  useIsomorphicLayoutEffect(() => {
    instanceRef.current?.setReadOnly(readOnly);
  }, [readOnly]);
  useIsomorphicLayoutEffect(() => {
    instanceRef.current?.setModal(modal);
  }, [modal]);
  useIsomorphicLayoutEffect(() => {
    instanceRef.current?.setHighlightItemOnHover(highlightItemOnHover);
  }, [highlightItemOnHover]);
  useIsomorphicLayoutEffect(() => {
    if (open === void 0) return;
    const instance = open ? ensureInstance() : instanceRef.current;
    if (!instance) return;
    if (instance.getOpen() === open) return;
    instance.setOpen(open, { emit: false });
  }, [ensureInstance, open]);
  useIsomorphicLayoutEffect(() => {
    if (value === void 0) return;
    const instance = instanceRef.current;
    if (!instance) return;
    if (instance.getValue() === value) return;
    instance.setValue(value, { emit: false });
  }, [value]);
  const renderedOpen = open ?? uncontrolledOpen;
  const selectedValue = value !== void 0 ? value : uncontrolledValue ?? null;
  const renderedValue = selectedValue ?? "";
  const [selectedLabel, setSelectedLabel] = React13.useState(() => ({ label: null, value: null }));
  React13.useEffect(() => {
    const timer = window.setTimeout(() => {
      setSelectedLabel({
        label: findSelectedOptionText(children, selectedValue),
        value: selectedValue
      });
    }, 0);
    return () => window.clearTimeout(timer);
  }, [children, selectedValue]);
  const renderedSelectedLabel = selectedLabel.value === selectedValue ? selectedLabel.label : null;
  const initializeFromTriggerEvent = React13.useCallback(
    (event) => {
      if (disabled) return;
      const target = event.target;
      if (!(target instanceof Element)) return;
      if (!target.closest("[data-sw-select-trigger]")) return;
      ensureInstance();
    },
    [ensureInstance, disabled]
  );
  const contextValue = React13.useMemo(
    () => ({
      disabled,
      open: renderedOpen,
      readOnly,
      required,
      value: selectedValue,
      selectedLabel: renderedSelectedLabel
    }),
    [disabled, renderedOpen, readOnly, required, selectedValue, renderedSelectedLabel]
  );
  return /* @__PURE__ */ jsx12(ReactPortalScopeProvider, { scope: portalScope, children: /* @__PURE__ */ jsx12(SelectContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxs(
    "div",
    {
      "data-sw-select": true,
      "data-autocomplete": autoComplete,
      "data-default-open": defaultOpenRef.current ? "true" : void 0,
      "data-default-value": defaultValueRef.current ?? void 0,
      "data-disabled": disabled ? "" : void 0,
      "data-form": form,
      "data-highlight-item-on-hover": highlightItemOnHover ? "true" : "false",
      "data-modal": modal ? "true" : "false",
      "data-name": name,
      "data-readonly": readOnly ? "" : void 0,
      "data-required": required ? "" : void 0,
      "data-value": selectedValue ?? void 0,
      "data-placeholder": selectedValue === null ? "" : void 0,
      "data-selected-value": renderedSelectedLabel !== null && selectedValue !== null ? selectedValue : void 0,
      "data-selected-label": renderedSelectedLabel ?? void 0,
      "data-state": renderedOpen ? "open" : "closed",
      ref: composedRef,
      ...props,
      onClickCapture: (event) => {
        props.onClickCapture?.(event);
        if (!event.defaultPrevented) initializeFromTriggerEvent(event);
      },
      onFocusCapture: (event) => {
        props.onFocusCapture?.(event);
        if (!event.defaultPrevented) initializeFromTriggerEvent(event);
      },
      onKeyDownCapture: (event) => {
        props.onKeyDownCapture?.(event);
        if (!event.defaultPrevented) initializeFromTriggerEvent(event);
      },
      onPointerDownCapture: (event) => {
        props.onPointerDownCapture?.(event);
        if (!event.defaultPrevented) initializeFromTriggerEvent(event);
      },
      children: [
        /* @__PURE__ */ jsx12(
          "input",
          {
            "data-sw-select-input": true,
            type: "hidden",
            autoComplete,
            form,
            name,
            disabled,
            required,
            value: renderedValue,
            "aria-hidden": "true",
            tabIndex: -1,
            readOnly: true
          }
        ),
        children
      ]
    }
  ) }) });
});
SelectRoot.displayName = "Select.Root";
var SelectRoot_default = SelectRoot;
function findSelectedOptionText(children, selectedValue) {
  if (selectedValue === null) return null;
  let selectedText = null;
  const visit = (node) => {
    if (selectedText !== null) return;
    React13.Children.forEach(node, (child) => {
      if (selectedText !== null || !React13.isValidElement(child)) return;
      const childProps = child.props;
      if (childProps.value === selectedValue) {
        selectedText = getSelectedOptionTextFromProps(childProps);
        return;
      }
      visit(childProps.children);
    });
  };
  visit(children);
  return selectedText;
}
function getSelectedOptionTextFromProps(props) {
  const explicitText = getStringPropText(props.textValue) ?? getStringPropText(props.label) ?? getStringPropText(props["aria-label"]);
  if (explicitText !== null) return explicitText;
  const itemText = getSelectItemTextFromReactNode(props.children);
  if (itemText !== null) return itemText;
  const childText = getTextFromReactNode(props.children).trim();
  return childText.length > 0 ? childText : null;
}
function getSelectItemTextFromReactNode(node) {
  let selectedText = null;
  const visit = (candidate) => {
    if (selectedText !== null) return;
    React13.Children.forEach(candidate, (child) => {
      if (selectedText !== null || !React13.isValidElement(child)) return;
      const childProps = child.props;
      if (isSelectItemTextElement(child)) {
        selectedText = getTextFromReactNode(childProps.children).trim();
        return;
      }
      visit(childProps.children);
    });
  };
  visit(node);
  return selectedText;
}
function getStringPropText(value) {
  if (typeof value !== "string" && typeof value !== "number") return null;
  const text = String(value).trim();
  return text.length > 0 ? text : null;
}
function isSelectItemTextElement(node) {
  const props = node.props;
  if (props["data-sw-select-item-text"] !== void 0) return true;
  const displayName = getReactComponentDisplayName(node.type);
  return displayName === "Select.ItemText" || displayName === "SelectItemText";
}
function getReactComponentDisplayName(type) {
  if (typeof type === "string") return null;
  if (typeof type !== "function" && (typeof type !== "object" || type === null)) return null;
  const candidate = type;
  return candidate.displayName ?? candidate.name ?? null;
}
function getTextFromReactNode(node) {
  if (node === null || node === void 0 || typeof node === "boolean") return "";
  if (typeof node === "string" || typeof node === "number") return String(node);
  if (!React13.isValidElement(node)) {
    let text = "";
    React13.Children.forEach(node, (child) => {
      text += getTextFromReactNode(child);
    });
    return text;
  }
  return getTextFromReactNode(node.props.children);
}
function getTextFromSelectItem(item) {
  if (!item) return null;
  const textElement = item.querySelector("[data-sw-select-item-text]");
  if (textElement) return textElement.textContent?.trim() ?? "";
  const text = item.textContent?.trim() ?? "";
  return text.length > 0 ? text : null;
}

// src/select/SelectScrollDownArrow.tsx
import * as React14 from "react";
import { jsx as jsx13 } from "react/jsx-runtime";
var SelectScrollDownArrow = React14.forwardRef(
  function SelectScrollDownArrow2(props, forwardedRef) {
    return /* @__PURE__ */ jsx13("div", { "data-sw-select-scroll-down-arrow": true, "aria-hidden": "true", ref: forwardedRef, ...props });
  }
);
SelectScrollDownArrow.displayName = "Select.ScrollDownArrow";
var SelectScrollDownArrow_default = SelectScrollDownArrow;

// src/select/SelectScrollUpArrow.tsx
import * as React15 from "react";
import { jsx as jsx14 } from "react/jsx-runtime";
var SelectScrollUpArrow = React15.forwardRef(
  function SelectScrollUpArrow2(props, forwardedRef) {
    return /* @__PURE__ */ jsx14("div", { "data-sw-select-scroll-up-arrow": true, "aria-hidden": "true", ref: forwardedRef, ...props });
  }
);
SelectScrollUpArrow.displayName = "Select.ScrollUpArrow";
var SelectScrollUpArrow_default = SelectScrollUpArrow;

// src/select/SelectSeparator.tsx
import * as React16 from "react";
import { jsx as jsx15 } from "react/jsx-runtime";
var SelectSeparator = React16.forwardRef(
  function SelectSeparator2(props, forwardedRef) {
    return /* @__PURE__ */ jsx15(
      "div",
      {
        "data-sw-select-separator": true,
        role: "separator",
        "aria-orientation": "horizontal",
        ref: forwardedRef,
        ...props
      }
    );
  }
);
SelectSeparator.displayName = "Select.Separator";
var SelectSeparator_default = SelectSeparator;

// src/select/SelectTrigger.tsx
import * as React17 from "react";
import { jsx as jsx16 } from "react/jsx-runtime";
var SelectTrigger = React17.forwardRef(function SelectTrigger2({ asChild = false, children, className, ...props }, forwardedRef) {
  const select = useSelectContext();
  const protectedTriggerProps = {
    "data-sw-select-trigger": "",
    "aria-haspopup": "listbox",
    "aria-expanded": select.open ? "true" : "false",
    "aria-disabled": select.disabled ? "true" : void 0,
    "aria-required": select.required ? "true" : void 0,
    "aria-readonly": select.readOnly ? "true" : "false",
    "data-disabled": select.disabled ? "" : void 0,
    "data-placeholder": select.value === null || select.selectedLabel === null ? "" : void 0,
    "data-required": select.required ? "" : void 0,
    "data-readonly": select.readOnly ? "" : void 0,
    "data-state": select.open ? "open" : "closed",
    disabled: select.disabled || void 0,
    role: "combobox"
  };
  const triggerProps = {
    ...protectedTriggerProps,
    ...props
  };
  const asChildElement = getAsChildElement(children);
  const composedRef = useComposedRefs(
    forwardedRef,
    asChildElement ? getElementRef(asChildElement) : void 0
  );
  if (asChild && asChildElement) {
    const child = asChildElement;
    const childProps = child.props;
    const mergedProps = mergeAsChildProps({ ...triggerProps, className }, childProps, {
      protectedProps: protectedTriggerProps
    });
    return React17.cloneElement(child, {
      ...mergedProps,
      ref: composedRef
    });
  }
  return /* @__PURE__ */ jsx16(
    "button",
    {
      type: "button",
      className,
      ref: forwardedRef,
      ...triggerProps,
      disabled: select.disabled,
      children
    }
  );
});
SelectTrigger.displayName = "Select.Trigger";
var SelectTrigger_default = SelectTrigger;

// src/select/SelectValue.tsx
import * as React18 from "react";
import { jsx as jsx17 } from "react/jsx-runtime";
var SelectValue = React18.forwardRef(function SelectValue2({ children, placeholder, ...props }, forwardedRef) {
  const select = useSelectContext();
  const fallback = select.value !== null && select.selectedLabel !== null ? select.selectedLabel : placeholder;
  return /* @__PURE__ */ jsx17("span", { "data-sw-select-value": true, "data-placeholder": placeholder, ref: forwardedRef, ...props, children: children ?? fallback });
});
SelectValue.displayName = "Select.Value";
var SelectValue_default = SelectValue;

// src/select/index.ts
var Select = {
  Root: SelectRoot_default,
  Label: SelectLabel_default,
  Trigger: SelectTrigger_default,
  Value: SelectValue_default,
  Icon: SelectIcon_default,
  Portal: SelectPortal_default,
  Positioner: SelectPositioner_default,
  Popup: SelectPopup_default,
  List: SelectList_default,
  Group: SelectGroup_default,
  GroupLabel: SelectGroupLabel_default,
  Item: SelectItem_default,
  ItemText: SelectItemText_default,
  ItemIndicator: SelectItemIndicator_default,
  Separator: SelectSeparator_default,
  ScrollUpArrow: SelectScrollUpArrow_default,
  ScrollDownArrow: SelectScrollDownArrow_default
};
var select_default = Select;

export {
  SelectGroup_default,
  SelectGroupLabel_default,
  SelectIcon_default,
  SelectContext,
  SelectItemContext,
  useSelectContext,
  useSelectItemContext,
  SelectItem_default,
  SelectItemIndicator_default,
  SelectItemText_default,
  SelectLabel_default,
  SelectList_default,
  SelectPopup_default,
  SelectPortal_default,
  SelectPositioner_default,
  SelectRoot_default,
  SelectScrollDownArrow_default,
  SelectScrollUpArrow_default,
  SelectSeparator_default,
  SelectTrigger_default,
  SelectValue_default,
  Select,
  select_default
};
