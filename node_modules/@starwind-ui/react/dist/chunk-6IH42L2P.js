import {
  setRef,
  useIsomorphicLayoutEffect
} from "./chunk-I44ESSZ2.js";

// src/slider/SliderControl.tsx
import * as React from "react";
import { jsx } from "react/jsx-runtime";
var SliderControl = React.forwardRef(
  function SliderControl2(props, forwardedRef) {
    return /* @__PURE__ */ jsx("div", { "data-sw-slider-control": true, ref: forwardedRef, ...props });
  }
);
SliderControl.displayName = "Slider.Control";
var SliderControl_default = SliderControl;

// src/slider/SliderIndicator.tsx
import * as React2 from "react";
import { jsx as jsx2 } from "react/jsx-runtime";
var SliderIndicator = React2.forwardRef(
  function SliderIndicator2(props, forwardedRef) {
    return /* @__PURE__ */ jsx2("div", { "data-sw-slider-indicator": true, ref: forwardedRef, ...props });
  }
);
SliderIndicator.displayName = "Slider.Indicator";
var SliderIndicator_default = SliderIndicator;

// src/slider/SliderLabel.tsx
import * as React3 from "react";
import { jsx as jsx3 } from "react/jsx-runtime";
var SliderLabel = React3.forwardRef(
  function SliderLabel2(props, forwardedRef) {
    return /* @__PURE__ */ jsx3("span", { "data-sw-slider-label": true, ref: forwardedRef, ...props });
  }
);
SliderLabel.displayName = "Slider.Label";
var SliderLabel_default = SliderLabel;

// src/slider/SliderRoot.tsx
import {
  createSlider
} from "@starwind-ui/runtime/slider";
import * as React4 from "react";
import { jsx as jsx4 } from "react/jsx-runtime";
var SliderRoot = React4.forwardRef(function SliderRoot2({
  defaultValue = 0,
  disabled = false,
  form,
  largeStep = 10,
  max = 100,
  min = 0,
  minStepsBetweenValues = 0,
  name,
  onValueChange,
  onValueCommitted,
  orientation = "horizontal",
  step = 1,
  value,
  ...props
}, forwardedRef) {
  const rootRef = React4.useRef(null);
  const instanceRef = React4.useRef(void 0);
  const defaultValueRef = React4.useRef(defaultValue);
  const disabledRef = React4.useRef(disabled);
  const formRef = React4.useRef(form);
  const largeStepRef = React4.useRef(largeStep);
  const maxRef = React4.useRef(max);
  const minRef = React4.useRef(min);
  const minStepsBetweenValuesRef = React4.useRef(minStepsBetweenValues);
  const nameRef = React4.useRef(name);
  const onValueChangeRef = React4.useRef(onValueChange);
  const onValueCommittedRef = React4.useRef(onValueCommitted);
  const orientationRef = React4.useRef(orientation);
  const stepRef = React4.useRef(step);
  const valueRef = React4.useRef(value);
  const [uncontrolledValue, setUncontrolledValue] = React4.useState(
    () => defaultValueRef.current
  );
  const uncontrolledValueRef = React4.useRef(uncontrolledValue);
  useIsomorphicLayoutEffect(() => {
    disabledRef.current = disabled;
  }, [disabled]);
  useIsomorphicLayoutEffect(() => {
    onValueChangeRef.current = onValueChange;
  }, [onValueChange]);
  useIsomorphicLayoutEffect(() => {
    onValueCommittedRef.current = onValueCommitted;
  }, [onValueCommitted]);
  useIsomorphicLayoutEffect(() => {
    valueRef.current = value;
  }, [value]);
  const composedRef = React4.useCallback(
    (node) => {
      rootRef.current = node;
      return setRef(forwardedRef, node);
    },
    [forwardedRef]
  );
  useIsomorphicLayoutEffect(() => {
    const root = rootRef.current;
    if (!root) return;
    const instance = createSlider(root, {
      defaultValue: defaultValueRef.current,
      disabled: disabledRef.current,
      form: formRef.current,
      largeStep: largeStepRef.current,
      max: maxRef.current,
      min: minRef.current,
      minStepsBetweenValues: minStepsBetweenValuesRef.current,
      name: nameRef.current,
      orientation: orientationRef.current,
      step: stepRef.current,
      onValueChange: (value2, details) => {
        onValueChangeRef.current?.(value2, details);
      },
      ...valueRef.current !== void 0 ? { value: valueRef.current } : {}
    });
    instanceRef.current = instance;
    const unsubscribeChange = instance.subscribe("valueChange", (details) => {
      if (details.isCanceled) return;
      if (valueRef.current === void 0) {
        uncontrolledValueRef.current = details.value;
        setUncontrolledValue(details.value);
      }
    });
    const unsubscribeCommitted = instance.subscribe("valueCommitted", (details) => {
      onValueCommittedRef.current?.(details.value, details);
    });
    return () => {
      unsubscribeChange();
      unsubscribeCommitted();
      instance.destroy();
      if (instanceRef.current === instance) {
        instanceRef.current = void 0;
      }
    };
  }, []);
  useIsomorphicLayoutEffect(() => {
    const instance = instanceRef.current;
    if (!instance) return;
    instance.setDisabled(disabled);
  }, [disabled]);
  useIsomorphicLayoutEffect(() => {
    nameRef.current = name;
    const instance = instanceRef.current;
    if (!instance) return;
    instance.setName(name);
  }, [name]);
  useIsomorphicLayoutEffect(() => {
    const instance = instanceRef.current;
    if (!instance) return;
    instance.setOptions({
      form,
      largeStep,
      max,
      min,
      minStepsBetweenValues,
      orientation,
      step
    });
    if (valueRef.current === void 0) {
      const nextUncontrolledValue = instance.getValue();
      if (!areSliderValuesEqual(uncontrolledValueRef.current, nextUncontrolledValue)) {
        uncontrolledValueRef.current = nextUncontrolledValue;
        setUncontrolledValue(nextUncontrolledValue);
      }
    }
  }, [form, largeStep, max, min, minStepsBetweenValues, orientation, step]);
  useIsomorphicLayoutEffect(() => {
    if (value === void 0) return;
    const instance = instanceRef.current;
    if (!instance) return;
    instance.refresh();
    if (areSliderValuesEqual(instance.getValue(), value)) return;
    instance.setValue(value, { emit: false });
  }, [value]);
  const renderedValue = value ?? uncontrolledValue;
  const valueAttribute = serializeSliderValue(renderedValue);
  const defaultValueAttribute = serializeSliderValue(defaultValueRef.current);
  return /* @__PURE__ */ jsx4(
    "div",
    {
      ...props,
      "data-sw-slider": true,
      "data-default-value": defaultValueAttribute,
      "data-disabled": disabled ? "" : void 0,
      "data-form": form,
      "data-large-step": largeStep,
      "data-max": max,
      "data-min": min,
      "data-min-steps-between-values": minStepsBetweenValues,
      "data-name": name,
      "data-orientation": orientation,
      "data-step": step,
      "data-value": valueAttribute,
      ref: composedRef,
      role: "group"
    }
  );
});
SliderRoot.displayName = "Slider.Root";
var SliderRoot_default = SliderRoot;
function areSliderValuesEqual(left, right) {
  const leftValues = Array.isArray(left) ? left : [left];
  const rightValues = Array.isArray(right) ? right : [right];
  return leftValues.length === rightValues.length && leftValues.every((item, index) => item === rightValues[index]);
}
function serializeSliderValue(value) {
  return Array.isArray(value) ? JSON.stringify(value) : String(value);
}

// src/slider/SliderThumb.tsx
import * as React5 from "react";
import { jsx as jsx5, jsxs } from "react/jsx-runtime";
var visuallyHiddenStyle = {
  border: 0,
  clipPath: "inset(50%)",
  height: "1px",
  margin: "-1px",
  overflow: "hidden",
  position: "absolute",
  whiteSpace: "nowrap",
  width: "1px"
};
var SliderThumb = React5.forwardRef(function SliderThumb2({ children, index, inputRef, ...props }, forwardedRef) {
  return /* @__PURE__ */ jsxs("div", { "data-sw-slider-thumb": true, "data-index": index, ref: forwardedRef, ...props, children: [
    children,
    /* @__PURE__ */ jsx5(
      "input",
      {
        "data-sw-slider-input": true,
        "aria-hidden": "true",
        ref: inputRef,
        style: visuallyHiddenStyle,
        tabIndex: -1,
        type: "range"
      }
    )
  ] });
});
SliderThumb.displayName = "Slider.Thumb";
var SliderThumb_default = SliderThumb;

// src/slider/SliderTrack.tsx
import * as React6 from "react";
import { jsx as jsx6 } from "react/jsx-runtime";
var SliderTrack = React6.forwardRef(
  function SliderTrack2(props, forwardedRef) {
    return /* @__PURE__ */ jsx6("div", { "data-sw-slider-track": true, ref: forwardedRef, ...props });
  }
);
SliderTrack.displayName = "Slider.Track";
var SliderTrack_default = SliderTrack;

// src/slider/index.ts
var Slider = {
  Root: SliderRoot_default,
  Control: SliderControl_default,
  Track: SliderTrack_default,
  Indicator: SliderIndicator_default,
  Label: SliderLabel_default,
  Thumb: SliderThumb_default
};
var slider_default = Slider;

export {
  SliderControl_default,
  SliderIndicator_default,
  SliderLabel_default,
  SliderRoot_default,
  SliderThumb_default,
  SliderTrack_default,
  Slider,
  slider_default
};
