import {
  setRef,
  useIsomorphicLayoutEffect
} from "./chunk-I44ESSZ2.js";

// src/avatar/AvatarFallback.tsx
import * as React from "react";
import { jsx } from "react/jsx-runtime";
var AvatarFallback = React.forwardRef(
  function AvatarFallback2({ delay, hidden, ...props }, forwardedRef) {
    const composedRef = React.useCallback(
      (node) => {
        if (node) {
          node.hidden = hidden ?? delay !== void 0;
        }
        return setRef(forwardedRef, node);
      },
      [delay, forwardedRef, hidden]
    );
    return /* @__PURE__ */ jsx(
      "span",
      {
        "data-sw-avatar-fallback": true,
        "data-delay": delay,
        "data-image-loading-status": "idle",
        ref: composedRef,
        ...props
      }
    );
  }
);
AvatarFallback.displayName = "Avatar.Fallback";
var AvatarFallback_default = AvatarFallback;

// src/avatar/AvatarImage.tsx
import * as React2 from "react";
import { jsx as jsx2 } from "react/jsx-runtime";
var AvatarImage = React2.forwardRef(function AvatarImage2({ onLoadingStatusChange, style, ...props }, forwardedRef) {
  const imageRef = React2.useRef(null);
  const onLoadingStatusChangeRef = React2.useRef(onLoadingStatusChange);
  const hasLoadingStatusChangeCallback = onLoadingStatusChange !== void 0;
  onLoadingStatusChangeRef.current = onLoadingStatusChange;
  const composedRef = React2.useCallback(
    (node) => {
      imageRef.current = node;
      return setRef(forwardedRef, node);
    },
    [forwardedRef]
  );
  React2.useEffect(() => {
    if (!hasLoadingStatusChangeCallback) return;
    const root = imageRef.current?.closest("[data-sw-avatar]");
    if (!root) return;
    const handleLoadingStatusChange = (event) => {
      const details = event.detail;
      onLoadingStatusChangeRef.current?.(details.status, details);
    };
    const notifyCurrentLoadingStatus = () => {
      const status = root.getAttribute(
        "data-image-loading-status"
      );
      if (!status || status === "idle") return;
      onLoadingStatusChangeRef.current?.(status, { previousStatus: "idle", status });
    };
    root.addEventListener("starwind:loading-status-change", handleLoadingStatusChange);
    notifyCurrentLoadingStatus();
    return () => {
      root.removeEventListener("starwind:loading-status-change", handleLoadingStatusChange);
    };
  }, [hasLoadingStatusChangeCallback]);
  return /* @__PURE__ */ jsx2(
    "img",
    {
      "data-sw-avatar-image": true,
      "data-image-loading-status": "idle",
      ref: composedRef,
      style: { ...style, visibility: "hidden" },
      ...props,
      hidden: false
    }
  );
});
AvatarImage.displayName = "Avatar.Image";
var AvatarImage_default = AvatarImage;

// src/avatar/AvatarRoot.tsx
import { createAvatar } from "@starwind-ui/runtime/avatar";
import * as React3 from "react";
import { jsx as jsx3 } from "react/jsx-runtime";
var AvatarRoot = React3.forwardRef(
  function AvatarRoot2(props, forwardedRef) {
    const rootRef = React3.useRef(null);
    const composedRef = React3.useCallback(
      (node) => {
        rootRef.current = node;
        return setRef(forwardedRef, node);
      },
      [forwardedRef]
    );
    useIsomorphicLayoutEffect(() => {
      const root = rootRef.current;
      if (!root) return;
      const instance = createAvatar(root);
      return () => {
        instance.destroy();
      };
    }, []);
    return /* @__PURE__ */ jsx3("span", { "data-sw-avatar": true, "data-image-loading-status": "idle", ref: composedRef, ...props });
  }
);
AvatarRoot.displayName = "Avatar.Root";
var AvatarRoot_default = AvatarRoot;

// src/avatar/index.ts
var Avatar = {
  Root: AvatarRoot_default,
  Image: AvatarImage_default,
  Fallback: AvatarFallback_default
};
var avatar_default = Avatar;

export {
  AvatarFallback_default,
  AvatarImage_default,
  AvatarRoot_default,
  Avatar,
  avatar_default
};
