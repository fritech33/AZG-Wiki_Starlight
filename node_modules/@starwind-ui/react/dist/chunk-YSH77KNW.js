import {
  CheckboxGroupContext
} from "./chunk-6C2ARQ7H.js";
import {
  setRef,
  useIsomorphicLayoutEffect
} from "./chunk-I44ESSZ2.js";

// src/checkbox-group/CheckboxGroupRoot.tsx
import {
  createCheckboxGroup
} from "@starwind-ui/runtime/checkbox-group";
import * as React from "react";
import { jsx } from "react/jsx-runtime";
var CheckboxGroupRoot = React.forwardRef(
  function CheckboxGroupRoot2({ defaultValue, disabled = false, onValueChange, value, ...props }, forwardedRef) {
    const rootRef = React.useRef(null);
    const instanceRef = React.useRef(void 0);
    const defaultValueRef = React.useRef(defaultValue);
    const disabledRef = React.useRef(disabled);
    const valueRef = React.useRef(value);
    const onValueChangeRef = React.useRef(onValueChange);
    const [uncontrolledValue, setUncontrolledValueState] = React.useState(
      () => defaultValueRef.current ?? []
    );
    const uncontrolledValueRef = React.useRef(uncontrolledValue);
    const setUncontrolledValue = React.useCallback((nextValue) => {
      if (areValuesEqual(uncontrolledValueRef.current, nextValue)) return;
      uncontrolledValueRef.current = nextValue;
      setUncontrolledValueState(nextValue);
    }, []);
    useIsomorphicLayoutEffect(() => {
      disabledRef.current = disabled;
    }, [disabled]);
    useIsomorphicLayoutEffect(() => {
      valueRef.current = value;
    }, [value]);
    useIsomorphicLayoutEffect(() => {
      onValueChangeRef.current = onValueChange;
    }, [onValueChange]);
    const composedRef = React.useCallback(
      (node) => {
        rootRef.current = node;
        return setRef(forwardedRef, node);
      },
      [forwardedRef]
    );
    useIsomorphicLayoutEffect(() => {
      const root = rootRef.current;
      if (!root) return;
      const instance = createCheckboxGroup(root, {
        defaultValue: defaultValueRef.current,
        disabled: disabledRef.current,
        onValueChange: (details) => {
          onValueChangeRef.current?.(details.value, details);
        },
        ...valueRef.current !== void 0 ? { value: valueRef.current } : {}
      });
      instanceRef.current = instance;
      const unsubscribe = instance.subscribe("valueChange", (details) => {
        if (details.isCanceled) return;
        if (valueRef.current === void 0) {
          setUncontrolledValue(details.value);
        }
      });
      return () => {
        unsubscribe();
        instance.destroy();
        if (instanceRef.current === instance) {
          instanceRef.current = void 0;
        }
      };
    }, []);
    useIsomorphicLayoutEffect(() => {
      const root = rootRef.current;
      if (!root || typeof MutationObserver === "undefined") return;
      const syncUncontrolledValue = () => {
        if (valueRef.current !== void 0) return;
        setUncontrolledValue(parseCheckboxGroupValueAttribute(root.getAttribute("data-value")));
      };
      const observer = new MutationObserver(syncUncontrolledValue);
      observer.observe(root, { attributes: true, attributeFilter: ["data-value"] });
      syncUncontrolledValue();
      return () => {
        observer.disconnect();
      };
    }, [setUncontrolledValue]);
    useIsomorphicLayoutEffect(() => {
      const instance = instanceRef.current;
      if (!instance) return;
      instance.setDisabled(disabled);
    }, [disabled]);
    useIsomorphicLayoutEffect(() => {
      if (value === void 0) return;
      const instance = instanceRef.current;
      if (!instance) return;
      if (areValuesEqual(instance.getValue(), value)) return;
      instance.setValue(value, { emit: false });
    }, [value]);
    const renderedValue = value ?? uncontrolledValue;
    const contextValue = React.useMemo(
      () => ({ disabled, value: renderedValue }),
      [disabled, renderedValue]
    );
    return /* @__PURE__ */ jsx(CheckboxGroupContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsx(
      "div",
      {
        "data-sw-checkbox-group": true,
        "data-default-value": defaultValueRef.current ? JSON.stringify(defaultValueRef.current) : void 0,
        "data-value": JSON.stringify(renderedValue),
        "data-disabled": disabled ? "" : void 0,
        ref: composedRef,
        role: "group",
        ...props
      }
    ) });
  }
);
CheckboxGroupRoot.displayName = "CheckboxGroup.Root";
var CheckboxGroupRoot_default = CheckboxGroupRoot;
function areValuesEqual(left, right) {
  return left.length === right.length && left.every((value, index) => value === right[index]);
}
function parseCheckboxGroupValueAttribute(value) {
  if (!value) return [];
  try {
    const parsed = JSON.parse(value);
    if (!Array.isArray(parsed)) return [];
    return parsed.filter((item) => typeof item === "string");
  } catch {
    return [];
  }
}

// src/checkbox-group/index.ts
var CheckboxGroup = {
  Root: CheckboxGroupRoot_default
};
var checkbox_group_default = CheckboxGroup;

export {
  CheckboxGroupRoot_default,
  CheckboxGroup,
  checkbox_group_default
};
