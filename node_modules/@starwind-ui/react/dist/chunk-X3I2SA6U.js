import {
  setRef,
  useIsomorphicLayoutEffect
} from "./chunk-I44ESSZ2.js";

// src/button/ButtonRoot.tsx
import { createButton } from "@starwind-ui/runtime/button";
import * as React from "react";
import { jsx } from "react/jsx-runtime";
var ButtonRoot = React.forwardRef(function ButtonRoot2({ disabled = false, focusableWhenDisabled = false, type, ...props }, forwardedRef) {
  const rootRef = React.useRef(null);
  const instanceRef = React.useRef(null);
  const disabledRef = React.useRef(disabled);
  disabledRef.current = disabled;
  const composedRef = React.useCallback(
    (node) => {
      rootRef.current = node;
      return setRef(forwardedRef, node);
    },
    [forwardedRef]
  );
  useIsomorphicLayoutEffect(() => {
    if (!focusableWhenDisabled) {
      instanceRef.current?.destroy();
      instanceRef.current = null;
      return;
    }
    const root = rootRef.current;
    if (!root) return;
    const instance = createButton(root, {
      disabled: disabledRef.current
    });
    instanceRef.current = instance;
    return () => {
      if (instanceRef.current === instance) {
        instanceRef.current = null;
      }
      instance.destroy();
    };
  }, [focusableWhenDisabled]);
  useIsomorphicLayoutEffect(() => {
    instanceRef.current?.setDisabled(disabled);
  }, [disabled]);
  return /* @__PURE__ */ jsx(
    "button",
    {
      "data-focusable-when-disabled": focusableWhenDisabled ? "true" : void 0,
      "aria-disabled": disabled && focusableWhenDisabled ? "true" : void 0,
      "data-disabled": disabled ? "" : void 0,
      disabled: disabled && !focusableWhenDisabled,
      ref: composedRef,
      type: type ?? "button",
      ...props,
      "data-sw-button": true
    }
  );
});
ButtonRoot.displayName = "Button.Root";
var ButtonRoot_default = ButtonRoot;

// src/button/index.ts
var Button = {
  Root: ButtonRoot_default
};
var button_default = Button;

export {
  ButtonRoot_default,
  Button,
  button_default
};
