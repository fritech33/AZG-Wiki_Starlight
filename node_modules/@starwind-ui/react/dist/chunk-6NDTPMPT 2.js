import {
  useClosePresence
} from "./chunk-4ZBBDK5W.js";
import {
  ReactPortal,
  ReactPortalScopeProvider,
  useReactPortalScope
} from "./chunk-PUUZDX3Q.js";
import {
  getAsChildElement,
  getElementRef,
  mergeAsChildProps,
  setRef,
  useComposedRefs,
  useIsomorphicLayoutEffect
} from "./chunk-I44ESSZ2.js";

// src/combobox/ComboboxClear.tsx
import * as React2 from "react";

// src/combobox/ComboboxContext.tsx
import * as React from "react";
var fallbackComboboxContext = {
  disabled: false,
  inputValue: "",
  open: false,
  readOnly: false,
  required: false,
  selectedText: null,
  value: null
};
var fallbackComboboxItemContext = {
  value: ""
};
var ComboboxContext = React.createContext(null);
var ComboboxItemContext = React.createContext(null);
function useComboboxContext() {
  return React.useContext(ComboboxContext) ?? fallbackComboboxContext;
}
function useComboboxItemContext() {
  return React.useContext(ComboboxItemContext) ?? fallbackComboboxItemContext;
}

// src/combobox/ComboboxClear.tsx
import { jsx } from "react/jsx-runtime";
var ComboboxClear = React2.forwardRef(function ComboboxClear2({ asChild = false, children, className, ...props }, forwardedRef) {
  const combobox = useComboboxContext();
  const protectedClearProps = {
    "data-sw-combobox-clear": "",
    "aria-disabled": combobox.disabled ? "true" : void 0,
    "aria-readonly": combobox.readOnly ? "true" : void 0,
    "data-disabled": combobox.disabled ? "" : void 0,
    "data-readonly": combobox.readOnly ? "" : void 0,
    disabled: combobox.disabled || void 0
  };
  const clearProps = {
    ...protectedClearProps,
    ...props
  };
  const asChildElement = getAsChildElement(children);
  const composedRef = useComposedRefs(
    forwardedRef,
    asChildElement ? getElementRef(asChildElement) : void 0
  );
  if (asChild && asChildElement) {
    const child = asChildElement;
    const childProps = child.props;
    const mergedProps = mergeAsChildProps({ ...clearProps, className }, childProps, {
      protectedProps: protectedClearProps
    });
    return React2.cloneElement(child, {
      ...mergedProps,
      ref: composedRef
    });
  }
  return /* @__PURE__ */ jsx(
    "button",
    {
      type: "button",
      className,
      ref: forwardedRef,
      ...clearProps,
      disabled: combobox.disabled,
      children
    }
  );
});
ComboboxClear.displayName = "Combobox.Clear";
var ComboboxClear_default = ComboboxClear;

// src/combobox/ComboboxEmpty.tsx
import * as React3 from "react";
import { jsx as jsx2 } from "react/jsx-runtime";
var ComboboxEmpty = React3.forwardRef(
  function ComboboxEmpty2(props, forwardedRef) {
    return /* @__PURE__ */ jsx2("div", { "data-sw-combobox-empty": true, hidden: true, ref: forwardedRef, ...props });
  }
);
ComboboxEmpty.displayName = "Combobox.Empty";
var ComboboxEmpty_default = ComboboxEmpty;

// src/combobox/ComboboxGroup.tsx
import * as React4 from "react";
import { jsx as jsx3 } from "react/jsx-runtime";
var ComboboxGroup = React4.forwardRef(
  function ComboboxGroup2(props, forwardedRef) {
    return /* @__PURE__ */ jsx3("div", { "data-sw-combobox-group": true, role: "group", ref: forwardedRef, ...props });
  }
);
ComboboxGroup.displayName = "Combobox.Group";
var ComboboxGroup_default = ComboboxGroup;

// src/combobox/ComboboxGroupLabel.tsx
import * as React5 from "react";
import { jsx as jsx4 } from "react/jsx-runtime";
var ComboboxGroupLabel = React5.forwardRef(
  function ComboboxGroupLabel2(props, forwardedRef) {
    return /* @__PURE__ */ jsx4("div", { "data-sw-combobox-group-label": true, ref: forwardedRef, ...props });
  }
);
ComboboxGroupLabel.displayName = "Combobox.GroupLabel";
var ComboboxGroupLabel_default = ComboboxGroupLabel;

// src/combobox/ComboboxIcon.tsx
import * as React6 from "react";
import { jsx as jsx5 } from "react/jsx-runtime";
var ComboboxIcon = React6.forwardRef(
  function ComboboxIcon2(props, forwardedRef) {
    return /* @__PURE__ */ jsx5("span", { "data-sw-combobox-icon": true, "aria-hidden": "true", ref: forwardedRef, ...props });
  }
);
ComboboxIcon.displayName = "Combobox.Icon";
var ComboboxIcon_default = ComboboxIcon;

// src/combobox/ComboboxInput.tsx
import * as React7 from "react";
import { jsx as jsx6 } from "react/jsx-runtime";
var ComboboxInput = React7.forwardRef(
  function ComboboxInput2(props, forwardedRef) {
    const combobox = useComboboxContext();
    const inputDisabled = combobox.disabled || props.disabled === true;
    const inputReadOnly = combobox.readOnly || props.readOnly === true;
    return /* @__PURE__ */ jsx6(
      "input",
      {
        ...props,
        "data-sw-combobox-input": true,
        role: "combobox",
        "aria-autocomplete": "list",
        "aria-disabled": inputDisabled ? "true" : void 0,
        "aria-expanded": combobox.open ? "true" : "false",
        "aria-readonly": inputReadOnly ? "true" : "false",
        "aria-required": combobox.required ? "true" : "false",
        autoComplete: "off",
        "data-disabled": inputDisabled ? "" : void 0,
        "data-readonly": inputReadOnly ? "" : void 0,
        "data-required": combobox.required ? "" : void 0,
        disabled: inputDisabled,
        readOnly: inputReadOnly,
        ref: forwardedRef,
        value: props.value ?? combobox.inputValue,
        onChange: props.onChange ?? noopComboboxInputChange
      }
    );
  }
);
ComboboxInput.displayName = "Combobox.Input";
var ComboboxInput_default = ComboboxInput;
function noopComboboxInputChange(_event) {
}

// src/combobox/ComboboxInputGroup.tsx
import * as React8 from "react";
import { jsx as jsx7 } from "react/jsx-runtime";
var ComboboxInputGroup = React8.forwardRef(
  function ComboboxInputGroup2(props, forwardedRef) {
    return /* @__PURE__ */ jsx7("div", { "data-sw-combobox-input-group": true, ref: forwardedRef, ...props });
  }
);
ComboboxInputGroup.displayName = "Combobox.InputGroup";
var ComboboxInputGroup_default = ComboboxInputGroup;

// src/combobox/ComboboxItem.tsx
import * as React9 from "react";
import { jsx as jsx8 } from "react/jsx-runtime";
var ComboboxItem = React9.forwardRef(function ComboboxItem2({ disabled = false, value, ...props }, forwardedRef) {
  const combobox = useComboboxContext();
  const selected = combobox.value === value;
  const itemContextValue = React9.useMemo(() => ({ value }), [value]);
  return /* @__PURE__ */ jsx8(ComboboxItemContext.Provider, { value: itemContextValue, children: /* @__PURE__ */ jsx8(
    "div",
    {
      "data-sw-combobox-item": true,
      "data-value": value,
      role: "option",
      "aria-selected": selected,
      "aria-disabled": disabled || void 0,
      "data-disabled": disabled ? "" : void 0,
      "data-selected": selected ? "" : void 0,
      tabIndex: -1,
      ref: forwardedRef,
      ...props
    }
  ) });
});
ComboboxItem.displayName = "Combobox.Item";
var ComboboxItem_default = ComboboxItem;

// src/combobox/ComboboxItemIndicator.tsx
import * as React10 from "react";
import { jsx as jsx9 } from "react/jsx-runtime";
var ComboboxItemIndicator = React10.forwardRef(
  function ComboboxItemIndicator2(props, forwardedRef) {
    const combobox = useComboboxContext();
    const item = useComboboxItemContext();
    const selected = combobox.value === item.value;
    return /* @__PURE__ */ jsx9(
      "span",
      {
        "data-sw-combobox-item-indicator": true,
        "aria-hidden": "true",
        "data-state": selected ? "checked" : "unchecked",
        "data-visible": selected ? "" : void 0,
        "data-hidden": selected ? void 0 : "",
        hidden: !selected,
        ref: forwardedRef,
        ...props
      }
    );
  }
);
ComboboxItemIndicator.displayName = "Combobox.ItemIndicator";
var ComboboxItemIndicator_default = ComboboxItemIndicator;

// src/combobox/ComboboxItemText.tsx
import * as React11 from "react";
import { jsx as jsx10 } from "react/jsx-runtime";
var ComboboxItemText = React11.forwardRef(
  function ComboboxItemText2(props, forwardedRef) {
    return /* @__PURE__ */ jsx10("span", { "data-sw-combobox-item-text": true, ref: forwardedRef, ...props });
  }
);
ComboboxItemText.displayName = "Combobox.ItemText";
var ComboboxItemText_default = ComboboxItemText;

// src/combobox/ComboboxLabel.tsx
import * as React12 from "react";
import { jsx as jsx11 } from "react/jsx-runtime";
var ComboboxLabel = React12.forwardRef(
  function ComboboxLabel2(props, forwardedRef) {
    return /* @__PURE__ */ jsx11("div", { "data-sw-combobox-label": true, ref: forwardedRef, ...props });
  }
);
ComboboxLabel.displayName = "Combobox.Label";
var ComboboxLabel_default = ComboboxLabel;

// src/combobox/ComboboxList.tsx
import * as React13 from "react";
import { jsx as jsx12 } from "react/jsx-runtime";
var ComboboxList = React13.forwardRef(
  function ComboboxList2(props, forwardedRef) {
    return /* @__PURE__ */ jsx12("div", { "data-sw-combobox-list": true, ref: forwardedRef, ...props });
  }
);
ComboboxList.displayName = "Combobox.List";
var ComboboxList_default = ComboboxList;

// src/combobox/ComboboxPopup.tsx
import * as React14 from "react";
import { jsx as jsx13 } from "react/jsx-runtime";
var ComboboxPopup = React14.forwardRef(function ComboboxPopup2({
  align = "start",
  alignOffset = 0,
  avoidCollisions = true,
  keepMounted = false,
  side = "bottom",
  sideOffset = 6,
  ...props
}, forwardedRef) {
  const combobox = useComboboxContext();
  const closePresence = useClosePresence({
    keepMounted,
    open: combobox.open
  });
  const composedRef = useComposedRefs(forwardedRef, closePresence.ref);
  return /* @__PURE__ */ jsx13(
    "div",
    {
      "data-sw-combobox-popup": true,
      role: "listbox",
      tabIndex: -1,
      "data-state": combobox.open ? "open" : "closed",
      "data-side": side,
      "data-align": align,
      "data-side-offset": sideOffset,
      "data-align-offset": alignOffset,
      "data-avoid-collisions": avoidCollisions ? "true" : "false",
      hidden: closePresence.hidden,
      ref: composedRef,
      ...props,
      children: closePresence.present ? props.children : null
    }
  );
});
ComboboxPopup.displayName = "Combobox.Popup";
var ComboboxPopup_default = ComboboxPopup;

// src/combobox/ComboboxPortal.tsx
import { reportPortalPlacement, resolvePortalPlacement } from "@starwind-ui/runtime/combobox";
import * as React15 from "react";
import { jsx as jsx14 } from "react/jsx-runtime";
var ComboboxPortal = React15.forwardRef(
  function ComboboxPortal2(props, forwardedRef) {
    return /* @__PURE__ */ jsx14(
      ReactPortal,
      {
        discoveryAttribute: "data-sw-combobox-portal",
        reportPlacement: reportPortalPlacement,
        resolvePlacement: resolvePortalPlacement,
        ref: forwardedRef,
        ...props
      }
    );
  }
);
ComboboxPortal.displayName = "Combobox.Portal";
var ComboboxPortal_default = ComboboxPortal;

// src/combobox/ComboboxPositioner.tsx
import * as React16 from "react";
import { jsx as jsx15 } from "react/jsx-runtime";
var ComboboxPositioner = React16.forwardRef(
  function ComboboxPositioner2({
    align = "start",
    alignOffset = 0,
    avoidCollisions = true,
    side = "bottom",
    sideOffset = 6,
    ...props
  }, forwardedRef) {
    return /* @__PURE__ */ jsx15(
      "div",
      {
        "data-sw-combobox-positioner": true,
        "data-state": "closed",
        "data-side": side,
        "data-align": align,
        "data-side-offset": sideOffset,
        "data-align-offset": alignOffset,
        "data-avoid-collisions": avoidCollisions ? "true" : "false",
        ref: forwardedRef,
        ...props
      }
    );
  }
);
ComboboxPositioner.displayName = "Combobox.Positioner";
var ComboboxPositioner_default = ComboboxPositioner;

// src/combobox/ComboboxRoot.tsx
import {
  createCombobox,
  createPortalBinding,
  refreshComboboxPortalSurface
} from "@starwind-ui/runtime/combobox";
import * as React17 from "react";
import { jsx as jsx16, jsxs } from "react/jsx-runtime";
var ComboboxRoot = React17.forwardRef(function ComboboxRoot2({
  autoComplete,
  defaultInputValue,
  defaultOpen = false,
  defaultValue,
  disabled = false,
  filterMode = "contains",
  form,
  highlightItemOnHover = true,
  inputValue,
  locale,
  modal = false,
  name,
  onInputValueChange,
  onOpenChange,
  onValueChange,
  open,
  readOnly = false,
  required = false,
  value,
  children,
  ...props
}, forwardedRef) {
  const rootRef = React17.useRef(null);
  const portalScope = useReactPortalScope(rootRef, createPortalBinding);
  const portalRuntimeActivation = portalScope.activation;
  const instanceRef = React17.useRef(void 0);
  const inputValueRef = React17.useRef(inputValue);
  const onInputValueChangeRef = React17.useRef(onInputValueChange);
  const onOpenChangeRef = React17.useRef(onOpenChange);
  const onValueChangeRef = React17.useRef(onValueChange);
  const openRef = React17.useRef(open);
  const valueRef = React17.useRef(value);
  const defaultInputValueRef = React17.useRef(defaultInputValue);
  const defaultOpenRef = React17.useRef(defaultOpen);
  const defaultValueRef = React17.useRef(defaultValue);
  const runtimeOptionsRef = React17.useRef({
    filterMode,
    highlightItemOnHover,
    locale,
    modal,
    readOnly
  });
  const pendingProgrammaticValueRef = React17.useRef(null);
  const [uncontrolledInputValue, setUncontrolledInputValueState] = React17.useState(
    defaultInputValueRef.current
  );
  const [uncontrolledOpen, setUncontrolledOpenState] = React17.useState(defaultOpenRef.current);
  const [uncontrolledValue, setUncontrolledValueState] = React17.useState(defaultValueRef.current);
  const [selectedInputValue, setSelectedInputValue] = React17.useState(() => ({ inputValue: null, value: null }));
  const uncontrolledInputValueRef = React17.useRef(uncontrolledInputValue);
  const uncontrolledOpenRef = React17.useRef(uncontrolledOpen);
  const uncontrolledValueRef = React17.useRef(uncontrolledValue);
  const setUncontrolledInputValue = React17.useCallback((nextInputValue) => {
    uncontrolledInputValueRef.current = nextInputValue;
    setUncontrolledInputValueState(nextInputValue);
  }, []);
  const setUncontrolledOpen = React17.useCallback((nextOpen) => {
    uncontrolledOpenRef.current = nextOpen;
    setUncontrolledOpenState(nextOpen);
  }, []);
  const setUncontrolledValue = React17.useCallback((nextValue) => {
    uncontrolledValueRef.current = nextValue;
    setUncontrolledValueState(nextValue);
  }, []);
  useIsomorphicLayoutEffect(() => {
    inputValueRef.current = inputValue;
  }, [inputValue]);
  useIsomorphicLayoutEffect(() => {
    onInputValueChangeRef.current = onInputValueChange;
  }, [onInputValueChange]);
  useIsomorphicLayoutEffect(() => {
    onOpenChangeRef.current = onOpenChange;
  }, [onOpenChange]);
  useIsomorphicLayoutEffect(() => {
    onValueChangeRef.current = onValueChange;
  }, [onValueChange]);
  useIsomorphicLayoutEffect(() => {
    openRef.current = open;
  }, [open]);
  useIsomorphicLayoutEffect(() => {
    valueRef.current = value;
  }, [value]);
  const composedRef = React17.useCallback(
    (node) => {
      rootRef.current = node;
      return setRef(forwardedRef, node);
    },
    [forwardedRef]
  );
  const ensureInstance = React17.useCallback(() => {
    const existing = instanceRef.current;
    if (existing) return existing;
    const root = rootRef.current;
    if (!root) return void 0;
    const selectedInitialValue = valueRef.current !== void 0 ? valueRef.current : uncontrolledValueRef.current ?? null;
    const selectedInitialInputValue = findSelectedComboboxItemText(children, selectedInitialValue);
    const defaultRuntimeInputValue = uncontrolledInputValueRef.current ?? selectedInitialInputValue ?? "";
    const defaultRuntimeFilterValue = uncontrolledInputValueRef.current ?? "";
    if (!portalScope.isReady()) return;
    const instance = createCombobox(root, {
      autoComplete,
      defaultInputValue: defaultRuntimeInputValue,
      ...inputValueRef.current === void 0 ? { defaultFilterValue: defaultRuntimeFilterValue } : {},
      ...selectedInitialInputValue !== null ? { defaultValueText: selectedInitialInputValue } : {},
      defaultOpen: uncontrolledOpenRef.current,
      defaultValue: uncontrolledValueRef.current,
      disabled,
      filterMode,
      form,
      highlightItemOnHover,
      locale,
      modal,
      name,
      onInputValueChange: (nextInputValue, details) => {
        onInputValueChangeRef.current?.(nextInputValue, details);
      },
      onOpenChange: (nextOpen, details) => {
        onOpenChangeRef.current?.(nextOpen, details);
      },
      onValueChange: (nextValue, details) => {
        onValueChangeRef.current?.(nextValue, details);
      },
      readOnly,
      required,
      ...inputValueRef.current !== void 0 ? { inputValue: inputValueRef.current } : {},
      ...openRef.current !== void 0 ? { open: openRef.current } : {},
      ...valueRef.current !== void 0 ? { value: valueRef.current } : {}
    });
    instanceRef.current = instance;
    instance.subscribe("inputValueChange", (details) => {
      if (inputValueRef.current === void 0) {
        setUncontrolledInputValue(details.inputValue);
      }
    });
    instance.subscribe("openChange", (details) => {
      if (openRef.current === void 0) {
        setUncontrolledOpen(details.open);
      }
    });
    instance.subscribe("valueChange", (details) => {
      const nextValue = details.value;
      const nextRuntimeInputValue = inputValueRef.current === void 0 ? instance.getInputValue() : void 0;
      const nextSelectedInputValue = getTextFromComboboxItem(details.item) ?? (nextValue === null ? null : findSelectedComboboxItemText(children, nextValue)) ?? nextRuntimeInputValue ?? null;
      if (nextSelectedInputValue !== null || nextValue === null) {
        setSelectedInputValue({
          inputValue: nextSelectedInputValue && nextSelectedInputValue.length > 0 ? nextSelectedInputValue : null,
          value: nextValue
        });
      }
      if (inputValueRef.current === void 0) {
        const nextInputValue = nextValue === null ? "" : nextSelectedInputValue ?? nextRuntimeInputValue ?? "";
        if (nextRuntimeInputValue !== nextInputValue) {
          instance.setInputValue(nextInputValue, {
            emit: false,
            filter: false
          });
        }
        setUncontrolledInputValue(nextInputValue);
      }
      if (valueRef.current === void 0) {
        setUncontrolledValue(nextValue);
      }
    });
    return instance;
  }, [
    children,
    autoComplete,
    disabled,
    filterMode,
    form,
    highlightItemOnHover,
    locale,
    modal,
    name,
    readOnly,
    required,
    setUncontrolledInputValue,
    setUncontrolledOpen,
    setUncontrolledValue,
    portalRuntimeActivation
  ]);
  useIsomorphicLayoutEffect(() => {
    if (!portalScope.isReady()) return;
    const root = rootRef.current;
    if (!root) return;
    refreshComboboxPortalSurface(root);
  }, [portalRuntimeActivation]);
  useIsomorphicLayoutEffect(() => {
    const root = rootRef.current;
    if (!root) return;
    const handleProgrammaticValueCommand = (event) => {
      if (!(event instanceof CustomEvent) || !event.detail || typeof event.detail !== "object")
        return;
      const detail = event.detail;
      if (!Object.hasOwn(detail, "value")) return;
      if (detail.value !== null && typeof detail.value !== "string") return;
      event.stopImmediatePropagation();
      const nextValue = detail.value === "" ? null : detail.value;
      const emit = typeof detail.emit === "boolean" ? detail.emit : void 0;
      const instance = ensureInstance();
      if (!instance) {
        pendingProgrammaticValueRef.current = { emit, value: nextValue };
        if (emit !== false || valueRef.current !== void 0) return;
        setUncontrolledValue(nextValue);
        if (inputValueRef.current === void 0) {
          const nextInputValue = nextValue === null ? "" : findSelectedComboboxItemText(children, nextValue) ?? "";
          setUncontrolledInputValue(nextInputValue);
          setSelectedInputValue({ inputValue: nextInputValue || null, value: nextValue });
        }
        return;
      }
      instance.setValue(nextValue, { emit });
      if (emit !== false) return;
      if (valueRef.current === void 0) {
        setUncontrolledValue(nextValue);
      }
      if (inputValueRef.current === void 0) {
        const nextInputValue = nextValue === null ? "" : findSelectedComboboxItemText(children, nextValue) ?? instance.getInputValue();
        instance.setInputValue(nextInputValue, {
          emit: false,
          filter: false
        });
        setUncontrolledInputValue(nextInputValue);
        setSelectedInputValue({ inputValue: nextInputValue || null, value: nextValue });
      }
    };
    root.addEventListener("starwind:set-value", handleProgrammaticValueCommand, {
      capture: true
    });
    return () => {
      root.removeEventListener("starwind:set-value", handleProgrammaticValueCommand, {
        capture: true
      });
    };
  }, [children, ensureInstance, setUncontrolledInputValue, setUncontrolledValue]);
  useIsomorphicLayoutEffect(() => {
    const pending = pendingProgrammaticValueRef.current;
    if (!pending) return;
    const instance = ensureInstance();
    if (!instance) return;
    pendingProgrammaticValueRef.current = null;
    instance.setValue(pending.value, { emit: pending.emit });
  }, [ensureInstance]);
  useIsomorphicLayoutEffect(() => {
    return () => {
      instanceRef.current?.destroy();
      instanceRef.current = void 0;
    };
  }, []);
  useIsomorphicLayoutEffect(() => {
    const nextRuntimeOptions = {
      filterMode,
      highlightItemOnHover,
      locale,
      modal,
      readOnly
    };
    const previousRuntimeOptions = runtimeOptionsRef.current;
    runtimeOptionsRef.current = nextRuntimeOptions;
    if (previousRuntimeOptions.filterMode === nextRuntimeOptions.filterMode && previousRuntimeOptions.highlightItemOnHover === nextRuntimeOptions.highlightItemOnHover && previousRuntimeOptions.locale === nextRuntimeOptions.locale && previousRuntimeOptions.modal === nextRuntimeOptions.modal && previousRuntimeOptions.readOnly === nextRuntimeOptions.readOnly) {
      return;
    }
    const instance = instanceRef.current;
    if (!instance) return;
    instance.destroy();
    if (instanceRef.current === instance) {
      instanceRef.current = void 0;
    }
    if ((openRef.current ?? uncontrolledOpenRef.current) === true) {
      ensureInstance();
    }
  }, [ensureInstance, filterMode, highlightItemOnHover, locale, modal, readOnly]);
  useIsomorphicLayoutEffect(() => {
    if ((openRef.current ?? uncontrolledOpenRef.current) !== true) return;
    ensureInstance();
  }, [ensureInstance, open, uncontrolledOpen]);
  useIsomorphicLayoutEffect(() => {
    instanceRef.current?.setFormOptions({ autoComplete, form, name, required });
  }, [autoComplete, form, name, required]);
  useIsomorphicLayoutEffect(() => {
    instanceRef.current?.setDisabled(disabled);
  }, [disabled]);
  useIsomorphicLayoutEffect(() => {
    if (inputValue === void 0) return;
    const instance = instanceRef.current;
    if (!instance) return;
    if (instance.getInputValue() === inputValue) return;
    instance.setInputValue(inputValue, { emit: false, filter: false });
  }, [inputValue]);
  useIsomorphicLayoutEffect(() => {
    if (open === void 0) return;
    const instance = open ? ensureInstance() : instanceRef.current;
    if (!instance) return;
    if (instance.getOpen() === open) return;
    instance.setOpen(open, { emit: false });
  }, [ensureInstance, open]);
  useIsomorphicLayoutEffect(() => {
    if (value === void 0) return;
    const instance = instanceRef.current;
    if (!instance) return;
    const previousValue = instance.getValue();
    if (previousValue !== value) {
      instance.setValue(value, { emit: false });
    }
    if (inputValueRef.current === void 0) {
      const nextInputValue = instance.getInputValue();
      if (uncontrolledInputValueRef.current !== nextInputValue) {
        setUncontrolledInputValue(nextInputValue);
      }
    }
  }, [setUncontrolledInputValue, value]);
  const selectedValue = value !== void 0 ? value : uncontrolledValue ?? null;
  useIsomorphicLayoutEffect(() => {
    const timer = window.setTimeout(() => {
      setSelectedInputValue({
        inputValue: findSelectedComboboxItemText(children, selectedValue),
        value: selectedValue
      });
    }, 0);
    return () => window.clearTimeout(timer);
  }, [children, selectedValue]);
  const selectedText = selectedInputValue.value === selectedValue ? selectedInputValue.inputValue : null;
  const renderedInputValue = inputValue !== void 0 ? inputValue : uncontrolledInputValue !== void 0 ? uncontrolledInputValue : selectedText ?? "";
  const renderedOpen = open ?? uncontrolledOpen;
  const renderedValue = selectedValue ?? "";
  const initializeFromInteractiveEvent = React17.useCallback(
    (event) => {
      if (disabled) return;
      const target = event.target;
      if (!(target instanceof Element)) return;
      if (!target.closest(
        "[data-sw-combobox-trigger], [data-sw-combobox-input], [data-sw-combobox-clear]"
      ))
        return;
      ensureInstance();
    },
    [ensureInstance, disabled]
  );
  const contextValue = React17.useMemo(
    () => ({
      disabled,
      inputValue: renderedInputValue ?? "",
      open: renderedOpen,
      readOnly,
      required,
      selectedText,
      value: selectedValue
    }),
    [disabled, renderedInputValue, renderedOpen, readOnly, required, selectedText, selectedValue]
  );
  return /* @__PURE__ */ jsx16(ReactPortalScopeProvider, { scope: portalScope, children: /* @__PURE__ */ jsx16(ComboboxContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsxs(
    "div",
    {
      "data-sw-combobox": true,
      "data-autocomplete": autoComplete,
      "data-default-input-value": defaultInputValueRef.current,
      "data-default-open": defaultOpenRef.current ? "true" : void 0,
      "data-default-value": defaultValueRef.current ?? void 0,
      "data-disabled": disabled ? "" : void 0,
      "data-filter-mode": filterMode,
      "data-form": form,
      "data-highlight-item-on-hover": highlightItemOnHover ? "true" : "false",
      "data-input-value": renderedInputValue,
      "data-locale": locale,
      "data-modal": modal ? "true" : "false",
      "data-name": name,
      "data-readonly": readOnly ? "" : void 0,
      "data-required": required ? "" : void 0,
      "data-value": selectedValue ?? void 0,
      "data-state": renderedOpen ? "open" : "closed",
      ref: composedRef,
      ...props,
      onClickCapture: (event) => {
        props.onClickCapture?.(event);
        if (!event.defaultPrevented) initializeFromInteractiveEvent(event);
      },
      onFocusCapture: (event) => {
        props.onFocusCapture?.(event);
        if (!event.defaultPrevented) initializeFromInteractiveEvent(event);
      },
      onInputCapture: (event) => {
        props.onInputCapture?.(event);
        if (!event.defaultPrevented) initializeFromInteractiveEvent(event);
      },
      onKeyDownCapture: (event) => {
        props.onKeyDownCapture?.(event);
        if (!event.defaultPrevented) initializeFromInteractiveEvent(event);
      },
      onPointerDownCapture: (event) => {
        props.onPointerDownCapture?.(event);
        if (!event.defaultPrevented) initializeFromInteractiveEvent(event);
      },
      children: [
        /* @__PURE__ */ jsx16(
          "input",
          {
            "data-sw-combobox-hidden-input": true,
            type: "hidden",
            autoComplete,
            form,
            name,
            disabled,
            required,
            value: renderedValue,
            "aria-hidden": "true",
            tabIndex: -1,
            readOnly: true
          }
        ),
        children
      ]
    }
  ) }) });
});
ComboboxRoot.displayName = "Combobox.Root";
var ComboboxRoot_default = ComboboxRoot;
function findSelectedComboboxItemText(children, selectedValue) {
  if (selectedValue === null) return null;
  let selectedText = null;
  const visit = (node) => {
    if (selectedText !== null) return;
    React17.Children.forEach(node, (child) => {
      if (selectedText !== null || !React17.isValidElement(child)) return;
      const childProps = child.props;
      if (childProps.value === selectedValue) {
        selectedText = getSelectedComboboxTextFromProps(childProps);
        return;
      }
      visit(childProps.children);
    });
  };
  visit(children);
  return selectedText;
}
function getSelectedComboboxTextFromProps(props) {
  const explicitText = getStringPropText(props.textValue) ?? getStringPropText(props.label) ?? getStringPropText(props["aria-label"]);
  if (explicitText !== null) return explicitText;
  const childText = getComboboxItemTextFromReactNode(props.children) ?? getTextFromReactNode(props.children).trim();
  return childText.length > 0 ? childText : null;
}
function getComboboxItemTextFromReactNode(node) {
  let selectedText = null;
  const visit = (candidate) => {
    if (selectedText !== null) return;
    React17.Children.forEach(candidate, (child) => {
      if (selectedText !== null || !React17.isValidElement(child)) return;
      const childProps = child.props;
      if (isComboboxItemTextElement(child)) {
        const text = getTextFromReactNode(childProps.children).trim();
        selectedText = text.length > 0 ? text : null;
        return;
      }
      visit(childProps.children);
    });
  };
  visit(node);
  return selectedText;
}
function getStringPropText(value) {
  if (typeof value !== "string" && typeof value !== "number") return null;
  const text = String(value).trim();
  return text.length > 0 ? text : null;
}
function isComboboxItemTextElement(node) {
  const props = node.props;
  if (props["data-sw-combobox-item-text"] !== void 0) return true;
  const displayName = getReactComponentDisplayName(node.type);
  return displayName === "Combobox.ItemText" || displayName === "ComboboxItemText";
}
function getReactComponentDisplayName(type) {
  if (typeof type === "string") return null;
  if (typeof type !== "function" && (typeof type !== "object" || type === null)) return null;
  const candidate = type;
  return candidate.displayName ?? candidate.name ?? null;
}
function getTextFromReactNode(node) {
  if (node === null || node === void 0 || typeof node === "boolean") return "";
  if (typeof node === "string" || typeof node === "number") return String(node);
  if (!React17.isValidElement(node)) {
    let text = "";
    React17.Children.forEach(node, (child) => {
      text += getTextFromReactNode(child);
    });
    return text;
  }
  return getTextFromReactNode(node.props.children);
}
function getTextFromComboboxItem(item) {
  if (!item) return null;
  const textElement = item.querySelector("[data-sw-combobox-item-text]");
  const text = (textElement ?? item).textContent?.trim() ?? "";
  return text.length > 0 ? text : null;
}

// src/combobox/ComboboxSeparator.tsx
import * as React18 from "react";
import { jsx as jsx17 } from "react/jsx-runtime";
var ComboboxSeparator = React18.forwardRef(
  function ComboboxSeparator2(props, forwardedRef) {
    return /* @__PURE__ */ jsx17(
      "div",
      {
        "data-sw-combobox-separator": true,
        role: "separator",
        "aria-orientation": "horizontal",
        ref: forwardedRef,
        ...props
      }
    );
  }
);
ComboboxSeparator.displayName = "Combobox.Separator";
var ComboboxSeparator_default = ComboboxSeparator;

// src/combobox/ComboboxTrigger.tsx
import * as React19 from "react";
import { jsx as jsx18 } from "react/jsx-runtime";
var ComboboxTrigger = React19.forwardRef(
  function ComboboxTrigger2({ asChild = false, children, className, ...props }, forwardedRef) {
    const combobox = useComboboxContext();
    const protectedTriggerProps = {
      "data-sw-combobox-trigger": "",
      "aria-disabled": combobox.disabled ? "true" : void 0,
      "aria-readonly": combobox.readOnly ? "true" : void 0,
      "data-disabled": combobox.disabled ? "" : void 0,
      "data-readonly": combobox.readOnly ? "" : void 0,
      "aria-haspopup": "listbox",
      "aria-expanded": combobox.open ? "true" : "false",
      "data-state": combobox.open ? "open" : "closed",
      disabled: combobox.disabled || void 0
    };
    const triggerProps = {
      ...protectedTriggerProps,
      ...props
    };
    const asChildElement = getAsChildElement(children);
    const composedRef = useComposedRefs(
      forwardedRef,
      asChildElement ? getElementRef(asChildElement) : void 0
    );
    if (asChild && asChildElement) {
      const child = asChildElement;
      const childProps = child.props;
      const mergedProps = mergeAsChildProps({ ...triggerProps, className }, childProps, {
        protectedProps: protectedTriggerProps
      });
      return React19.cloneElement(child, {
        ...mergedProps,
        ref: composedRef
      });
    }
    return /* @__PURE__ */ jsx18(
      "button",
      {
        type: "button",
        className,
        ref: forwardedRef,
        ...triggerProps,
        disabled: combobox.disabled,
        children
      }
    );
  }
);
ComboboxTrigger.displayName = "Combobox.Trigger";
var ComboboxTrigger_default = ComboboxTrigger;

// src/combobox/ComboboxValue.tsx
import * as React20 from "react";
import { jsx as jsx19 } from "react/jsx-runtime";
var ComboboxValue = React20.forwardRef(function ComboboxValue2({ placeholder, children, ...props }, forwardedRef) {
  const combobox = useComboboxContext();
  const selectedText = combobox.selectedText ?? "";
  const displayedChildren = children ?? (selectedText.length > 0 ? selectedText : placeholder);
  return /* @__PURE__ */ jsx19("span", { "data-sw-combobox-value": true, "data-placeholder": placeholder, ref: forwardedRef, ...props, children: displayedChildren });
});
ComboboxValue.displayName = "Combobox.Value";
var ComboboxValue_default = ComboboxValue;

// src/combobox/index.ts
var Combobox = {
  Root: ComboboxRoot_default,
  Label: ComboboxLabel_default,
  InputGroup: ComboboxInputGroup_default,
  Input: ComboboxInput_default,
  Trigger: ComboboxTrigger_default,
  Icon: ComboboxIcon_default,
  Clear: ComboboxClear_default,
  Value: ComboboxValue_default,
  Portal: ComboboxPortal_default,
  Positioner: ComboboxPositioner_default,
  Popup: ComboboxPopup_default,
  Empty: ComboboxEmpty_default,
  List: ComboboxList_default,
  Group: ComboboxGroup_default,
  GroupLabel: ComboboxGroupLabel_default,
  Item: ComboboxItem_default,
  ItemText: ComboboxItemText_default,
  ItemIndicator: ComboboxItemIndicator_default,
  Separator: ComboboxSeparator_default
};
var combobox_default = Combobox;

export {
  ComboboxContext,
  ComboboxItemContext,
  useComboboxContext,
  useComboboxItemContext,
  ComboboxClear_default,
  ComboboxEmpty_default,
  ComboboxGroup_default,
  ComboboxGroupLabel_default,
  ComboboxIcon_default,
  ComboboxInput_default,
  ComboboxInputGroup_default,
  ComboboxItem_default,
  ComboboxItemIndicator_default,
  ComboboxItemText_default,
  ComboboxLabel_default,
  ComboboxList_default,
  ComboboxPopup_default,
  ComboboxPortal_default,
  ComboboxPositioner_default,
  ComboboxRoot_default,
  ComboboxSeparator_default,
  ComboboxTrigger_default,
  ComboboxValue_default,
  Combobox,
  combobox_default
};
