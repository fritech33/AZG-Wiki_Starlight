import {
  useRadioGroupContext
} from "./chunk-RM2RNBLY.js";
import {
  setRef,
  useIsomorphicLayoutEffect
} from "./chunk-I44ESSZ2.js";

// src/radio/RadioIndicator.tsx
import * as React from "react";
import { jsx } from "react/jsx-runtime";
var RadioIndicator = React.forwardRef(
  function RadioIndicator2({ hidden, keepMounted = false, ...props }, forwardedRef) {
    const composedRef = React.useCallback(
      (node) => {
        if (node) {
          node.hidden = hidden ?? !keepMounted;
        }
        return setRef(forwardedRef, node);
      },
      [forwardedRef, hidden, keepMounted]
    );
    return /* @__PURE__ */ jsx(
      "span",
      {
        "data-sw-radio-indicator": true,
        "data-keep-mounted": keepMounted ? "true" : void 0,
        "data-unchecked": true,
        ref: composedRef,
        ...props
      }
    );
  }
);
RadioIndicator.displayName = "Radio.Indicator";
var RadioIndicator_default = RadioIndicator;

// src/radio/RadioRoot.tsx
import { createRadio } from "@starwind-ui/runtime/radio";
import * as React2 from "react";
import { Fragment, jsx as jsx2, jsxs } from "react/jsx-runtime";
var visuallyHiddenStyle = {
  border: 0,
  clip: "rect(0 0 0 0)",
  height: "1px",
  margin: "-1px",
  overflow: "hidden",
  position: "absolute",
  whiteSpace: "nowrap",
  width: "1px"
};
var RadioRoot = React2.forwardRef(
  function RadioRoot2({
    checked,
    children,
    defaultChecked = false,
    disabled = false,
    form,
    id,
    name,
    nativeButton = false,
    onCheckedChange,
    readOnly = false,
    required = false,
    value,
    ...props
  }, forwardedRef) {
    const rootRef = React2.useRef(null);
    const instanceRef = React2.useRef(void 0);
    const checkedRef = React2.useRef(checked);
    const onCheckedChangeRef = React2.useRef(onCheckedChange);
    const defaultCheckedRef = React2.useRef(defaultChecked);
    const radioGroup = useRadioGroupContext();
    const groupChecked = radioGroup && value !== void 0 ? radioGroup.value === value : void 0;
    const effectiveDisabled = disabled || radioGroup?.disabled === true;
    const effectiveForm = form ?? radioGroup?.form;
    const effectiveName = name ?? radioGroup?.name;
    const effectiveReadOnly = readOnly || radioGroup?.readOnly === true;
    const effectiveRequired = required || radioGroup?.required === true;
    const [uncontrolledChecked, setUncontrolledCheckedState] = React2.useState(
      groupChecked ?? defaultCheckedRef.current
    );
    const uncontrolledCheckedRef = React2.useRef(uncontrolledChecked);
    const setUncontrolledChecked = React2.useCallback((nextChecked) => {
      uncontrolledCheckedRef.current = nextChecked;
      setUncontrolledCheckedState(nextChecked);
    }, []);
    useIsomorphicLayoutEffect(() => {
      checkedRef.current = checked;
    }, [checked]);
    useIsomorphicLayoutEffect(() => {
      onCheckedChangeRef.current = onCheckedChange;
    }, [onCheckedChange]);
    const composedRef = React2.useCallback(
      (node) => {
        rootRef.current = node;
        return setRef(forwardedRef, node);
      },
      [forwardedRef]
    );
    useIsomorphicLayoutEffect(() => {
      const root = rootRef.current;
      if (!root) return;
      const instance = createRadio(root, {
        defaultChecked: uncontrolledCheckedRef.current,
        disabled: effectiveDisabled,
        form: effectiveForm,
        id,
        name: effectiveName,
        readOnly: effectiveReadOnly,
        required: effectiveRequired,
        value,
        onCheckedChange: (checked2, details) => {
          onCheckedChangeRef.current?.(checked2, details);
        },
        ...checkedRef.current !== void 0 ? { checked: checkedRef.current } : groupChecked !== void 0 ? { checked: groupChecked } : {}
      });
      instanceRef.current = instance;
      const unsubscribe = instance.subscribe("checkedChange", (details) => {
        details.onAccepted(() => {
          if (checkedRef.current === void 0 && radioGroup === void 0) {
            setUncontrolledChecked(details.checked);
          }
        });
      });
      const unsubscribeStateSync = instance.subscribe("stateSync", () => {
        if (checkedRef.current === void 0 && radioGroup === void 0) {
          setUncontrolledChecked(instance.getChecked());
        }
      });
      return () => {
        unsubscribeStateSync();
        unsubscribe();
        instance.destroy();
        if (instanceRef.current === instance) {
          instanceRef.current = void 0;
        }
      };
    }, [effectiveForm, effectiveName, id, nativeButton, value]);
    useIsomorphicLayoutEffect(() => {
      if (checked === void 0) return;
      const instance = instanceRef.current;
      if (!instance) return;
      if (instance.getChecked() === checked) return;
      instance.setChecked(checked, { emit: false });
    }, [checked]);
    useIsomorphicLayoutEffect(() => {
      if (groupChecked === void 0) return;
      const instance = instanceRef.current;
      if (!instance) return;
      if (instance.getChecked() === groupChecked) return;
      instance.setChecked(groupChecked, { emit: false });
    }, [groupChecked]);
    useIsomorphicLayoutEffect(() => {
      const instance = instanceRef.current;
      if (!instance) return;
      instance.setDisabled(effectiveDisabled);
    }, [effectiveDisabled]);
    useIsomorphicLayoutEffect(() => {
      const instance = instanceRef.current;
      if (!instance) return;
      instance.setReadOnly(effectiveReadOnly);
    }, [effectiveReadOnly]);
    useIsomorphicLayoutEffect(() => {
      const instance = instanceRef.current;
      if (!instance) return;
      instance.setFormOptions({
        form: effectiveForm,
        name: effectiveName,
        required: effectiveRequired,
        value
      });
    }, [effectiveForm, effectiveName, effectiveRequired, value]);
    const renderedChecked = checked ?? groupChecked ?? uncontrolledChecked;
    const commonProps = {
      "data-sw-radio": "",
      "data-default-checked": defaultCheckedRef.current ? "true" : void 0,
      "data-form": effectiveForm,
      "data-id": id,
      "data-name": effectiveName,
      "data-value": value,
      "aria-checked": renderedChecked,
      "data-checked": renderedChecked ? "" : void 0,
      "data-disabled": effectiveDisabled ? "" : void 0,
      "data-readonly": effectiveReadOnly ? "" : void 0,
      "data-required": effectiveRequired ? "" : void 0,
      "data-unchecked": !renderedChecked ? "" : void 0,
      role: "radio",
      tabIndex: effectiveDisabled ? -1 : 0
    };
    const input = /* @__PURE__ */ jsx2(
      "input",
      {
        "data-sw-radio-input": true,
        "aria-hidden": "true",
        defaultChecked: renderedChecked,
        defaultValue: value,
        disabled: effectiveDisabled,
        form: effectiveForm,
        id: nativeButton ? void 0 : id,
        name: effectiveName,
        required: effectiveRequired,
        style: visuallyHiddenStyle,
        tabIndex: -1,
        type: "radio"
      }
    );
    if (nativeButton) {
      return /* @__PURE__ */ jsxs(Fragment, { children: [
        /* @__PURE__ */ jsx2(
          "button",
          {
            ...props,
            ...commonProps,
            disabled: effectiveDisabled,
            id,
            ref: composedRef,
            type: "button",
            children
          }
        ),
        input
      ] });
    }
    return /* @__PURE__ */ jsxs(
      "span",
      {
        ...props,
        ...commonProps,
        ref: composedRef,
        children: [
          children,
          input
        ]
      }
    );
  }
);
RadioRoot.displayName = "Radio.Root";
var RadioRoot_default = RadioRoot;

// src/radio/index.ts
var Radio = {
  Root: RadioRoot_default,
  Indicator: RadioIndicator_default
};
var radio_default = Radio;

export {
  RadioIndicator_default,
  RadioRoot_default,
  Radio,
  radio_default
};
