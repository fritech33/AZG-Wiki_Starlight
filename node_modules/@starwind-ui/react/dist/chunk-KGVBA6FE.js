import {
  setRef,
  useIsomorphicLayoutEffect
} from "./chunk-I44ESSZ2.js";

// src/input/InputRoot.tsx
import {
  createInput
} from "@starwind-ui/runtime/input";
import * as React from "react";
import { jsx } from "react/jsx-runtime";
var InputRoot = React.forwardRef(function InputRoot2({ defaultValue, disabled = false, onChange, onValueChange, readOnly, value, ...props }, forwardedRef) {
  const rootRef = React.useRef(null);
  const instanceRef = React.useRef(void 0);
  const valueRef = React.useRef(value);
  const onValueChangeRef = React.useRef(onValueChange);
  const valueChangeDetailsRef = React.useRef(void 0);
  const controlledSyncTimerRef = React.useRef(void 0);
  const defaultValueRef = React.useRef(defaultValue);
  useIsomorphicLayoutEffect(() => {
    valueRef.current = value;
  }, [value]);
  useIsomorphicLayoutEffect(() => {
    onValueChangeRef.current = onValueChange;
  }, [onValueChange]);
  const composedRef = React.useCallback(
    (node) => {
      rootRef.current = node;
      return setRef(forwardedRef, node);
    },
    [forwardedRef]
  );
  useIsomorphicLayoutEffect(() => {
    const root = rootRef.current;
    if (!root) return;
    const instance = createInput(root, {
      defaultValue: defaultValueRef.current,
      disabled,
      onValueChange: (_nextValue, details) => {
        valueChangeDetailsRef.current = details;
      },
      ...valueRef.current !== void 0 ? { value: valueRef.current } : {}
    });
    instanceRef.current = instance;
    return () => {
      if (controlledSyncTimerRef.current !== void 0) {
        window.clearTimeout(controlledSyncTimerRef.current);
        controlledSyncTimerRef.current = void 0;
      }
      instance.destroy();
      if (instanceRef.current === instance) {
        instanceRef.current = void 0;
      }
    };
  }, []);
  useIsomorphicLayoutEffect(() => {
    if (value === void 0) return;
    const instance = instanceRef.current;
    if (!instance) return;
    if (instance.getValue() === String(value)) return;
    instance.setValue(value, { emit: false });
  }, [value]);
  useIsomorphicLayoutEffect(() => {
    const instance = instanceRef.current;
    if (!instance) return;
    instance.setDisabled(disabled);
  }, [disabled]);
  const scheduleControlledSync = React.useCallback(() => {
    if (valueRef.current === void 0) return;
    if (controlledSyncTimerRef.current !== void 0) {
      window.clearTimeout(controlledSyncTimerRef.current);
    }
    controlledSyncTimerRef.current = window.setTimeout(() => {
      controlledSyncTimerRef.current = void 0;
      const root = rootRef.current;
      const instance = instanceRef.current;
      if (!root || !instance) return;
      instance.setValue(root.value, { emit: false });
    }, 0);
  }, []);
  const handleChange = React.useCallback(
    (event) => {
      const nextValue = event.currentTarget.value;
      const details = valueChangeDetailsRef.current;
      onChange?.(event);
      if (details?.value === nextValue) {
        valueChangeDetailsRef.current = void 0;
        onValueChangeRef.current?.(nextValue, details);
      }
      scheduleControlledSync();
    },
    [onChange, scheduleControlledSync]
  );
  const valueProps = value !== void 0 ? { value } : { defaultValue: defaultValueRef.current };
  return /* @__PURE__ */ jsx(
    "input",
    {
      "data-sw-input": true,
      "data-disabled": disabled ? "" : void 0,
      disabled,
      onChange: handleChange,
      readOnly,
      ref: composedRef,
      ...valueProps,
      ...props
    }
  );
});
InputRoot.displayName = "Input.Root";
var InputRoot_default = InputRoot;

export {
  InputRoot_default
};
