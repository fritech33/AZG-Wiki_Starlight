import {
  setRef,
  useIsomorphicLayoutEffect
} from "./chunk-I44ESSZ2.js";

// src/toggle-group/ToggleGroupContext.tsx
import * as React from "react";
var ToggleGroupContext = React.createContext(void 0);
ToggleGroupContext.displayName = "ToggleGroupContext";
function useToggleGroupContext() {
  return React.useContext(ToggleGroupContext);
}

// src/toggle-group/ToggleGroupRoot.tsx
import {
  createToggleGroup
} from "@starwind-ui/runtime/toggle-group";
import * as React2 from "react";
import { jsx } from "react/jsx-runtime";
var ToggleGroupRoot = React2.forwardRef(
  function ToggleGroupRoot2({
    defaultValue,
    disabled = false,
    loopFocus = true,
    multiple = false,
    onValueChange,
    orientation = "horizontal",
    value,
    ...props
  }, forwardedRef) {
    const rootRef = React2.useRef(null);
    const instanceRef = React2.useRef(void 0);
    const defaultValueRef = React2.useRef(defaultValue);
    const disabledRef = React2.useRef(disabled);
    const loopFocusRef = React2.useRef(loopFocus);
    const multipleRef = React2.useRef(multiple);
    const onValueChangeRef = React2.useRef(onValueChange);
    const orientationRef = React2.useRef(orientation);
    const valueRef = React2.useRef(value);
    const [uncontrolledValue, setUncontrolledValue] = React2.useState(
      () => normalizeRenderedValue(defaultValueRef.current ?? [], multipleRef.current)
    );
    useIsomorphicLayoutEffect(() => {
      disabledRef.current = disabled;
    }, [disabled]);
    useIsomorphicLayoutEffect(() => {
      loopFocusRef.current = loopFocus;
    }, [loopFocus]);
    useIsomorphicLayoutEffect(() => {
      multipleRef.current = multiple;
    }, [multiple]);
    useIsomorphicLayoutEffect(() => {
      onValueChangeRef.current = onValueChange;
    }, [onValueChange]);
    useIsomorphicLayoutEffect(() => {
      orientationRef.current = orientation;
    }, [orientation]);
    useIsomorphicLayoutEffect(() => {
      valueRef.current = value;
    }, [value]);
    const composedRef = React2.useCallback(
      (node) => {
        rootRef.current = node;
        return setRef(forwardedRef, node);
      },
      [forwardedRef]
    );
    useIsomorphicLayoutEffect(() => {
      const root = rootRef.current;
      if (!root) return;
      const instance = createToggleGroup(root, {
        defaultValue: defaultValueRef.current,
        disabled: disabledRef.current,
        loopFocus: loopFocusRef.current,
        multiple: multipleRef.current,
        orientation: orientationRef.current,
        onValueChange: (value2, details) => {
          onValueChangeRef.current?.(value2, details);
        },
        ...valueRef.current !== void 0 ? { value: valueRef.current } : {}
      });
      instanceRef.current = instance;
      const unsubscribe = instance.subscribe("valueChange", (details) => {
        if (details.isCanceled) return;
        if (valueRef.current === void 0) {
          setUncontrolledValue(details.value);
        }
      });
      return () => {
        unsubscribe();
        instance.destroy();
        if (instanceRef.current === instance) {
          instanceRef.current = void 0;
        }
      };
    }, []);
    useIsomorphicLayoutEffect(() => {
      const instance = instanceRef.current;
      if (!instance) return;
      instance.setDisabled(disabled);
    }, [disabled]);
    useIsomorphicLayoutEffect(() => {
      const instance = instanceRef.current;
      if (!instance) return;
      instance.setLoopFocus(loopFocus);
    }, [loopFocus]);
    useIsomorphicLayoutEffect(() => {
      const instance = instanceRef.current;
      if (!instance) return;
      instance.setMultiple(multiple);
      if (valueRef.current === void 0) {
        setUncontrolledValue(instance.getValue());
      }
    }, [multiple]);
    useIsomorphicLayoutEffect(() => {
      const instance = instanceRef.current;
      if (!instance) return;
      instance.setOrientation(orientation);
    }, [orientation]);
    useIsomorphicLayoutEffect(() => {
      if (value === void 0) return;
      const instance = instanceRef.current;
      if (!instance) return;
      if (areValuesEqual(instance.getValue(), value)) return;
      instance.setValue(value, { emit: false });
    }, [multiple, value]);
    const renderedValue = React2.useMemo(
      () => normalizeRenderedValue(value ?? uncontrolledValue, multiple),
      [multiple, uncontrolledValue, value]
    );
    const contextValue = React2.useMemo(
      () => ({ disabled, loopFocus, multiple, orientation, value: renderedValue }),
      [disabled, loopFocus, multiple, orientation, renderedValue]
    );
    return /* @__PURE__ */ jsx(ToggleGroupContext.Provider, { value: contextValue, children: /* @__PURE__ */ jsx(
      "div",
      {
        "data-sw-toggle-group": true,
        "data-default-value": defaultValueRef.current ? JSON.stringify(normalizeRenderedValue(defaultValueRef.current, multipleRef.current)) : void 0,
        "data-disabled": disabled ? "" : void 0,
        "data-loop-focus": !loopFocus ? "false" : void 0,
        "data-multiple": multiple ? "" : void 0,
        "data-orientation": orientation,
        "data-value": JSON.stringify(renderedValue),
        ref: composedRef,
        role: "group",
        ...props
      }
    ) });
  }
);
ToggleGroupRoot.displayName = "ToggleGroup.Root";
var ToggleGroupRoot_default = ToggleGroupRoot;
function areValuesEqual(left, right) {
  return left.length === right.length && left.every((value, index) => value === right[index]);
}
function normalizeRenderedValue(value, multiple) {
  const values = Array.from(new Set(value.filter((item) => item.length > 0)));
  return multiple ? values : values.slice(0, 1);
}

// src/toggle-group/index.ts
var ToggleGroup = {
  Root: ToggleGroupRoot_default
};
var toggle_group_default = ToggleGroup;

export {
  ToggleGroupContext,
  useToggleGroupContext,
  ToggleGroupRoot_default,
  ToggleGroup,
  toggle_group_default
};
