type ToastVariant = "default" | "error" | "info" | "loading" | "success" | "warning";
type ToastPosition = "bottom-center" | "bottom-left" | "bottom-right" | "top-center" | "top-left" | "top-right";
type ToastAction = {
    label: string;
    onClick: () => void;
};
type ToastOptions = {
    action?: ToastAction;
    description?: string;
    duration?: number;
    id?: string;
    onClose?: () => void;
    onRemove?: () => void;
    title?: string;
    variant?: ToastVariant;
};
type ToastPromiseStateOption = {
    description?: string;
    duration?: number;
    title?: string;
};
type ToastPromiseStateValue<T> = ((data: T) => string | ToastPromiseStateOption) | ToastPromiseStateOption | string;
type ToastPromiseOptions<T, E = Error> = {
    error: ToastPromiseStateValue<E>;
    loading: string | ToastPromiseStateOption;
    success: ToastPromiseStateValue<T>;
};
type ToastManagerOptions = {
    defaultDuration?: number;
    limit?: number;
};
type ToastManager = {
    readonly viewport: HTMLElement;
    add(options: ToastOptions): string;
    close(id: string): void;
    closeAll(): void;
    destroy(): void;
    dismiss(id?: string): void;
    getToasts(): ToastState[];
    update(id: string, options: Partial<ToastOptions>): void;
};
type ToastApi = {
    (message: string, options?: Omit<ToastOptions, "title">): string;
    (options: ToastOptions): string;
    dismiss(id?: string): void;
    error(message: string, options?: Omit<ToastOptions, "variant">): string;
    info(message: string, options?: Omit<ToastOptions, "variant">): string;
    loading(message: string, options?: Omit<ToastOptions, "variant">): string;
    promise<T, E = Error>(promise: Promise<T>, options: ToastPromiseOptions<T, E>): Promise<T>;
    success(message: string, options?: Omit<ToastOptions, "variant">): string;
    update(id: string, options: Partial<ToastOptions>): void;
    warning(message: string, options?: Omit<ToastOptions, "variant">): string;
};
type ToastState = ToastOptions & {
    closing?: boolean;
    element?: HTMLElement;
    height?: number;
    id: string;
    managedDescribedBy?: string;
    managedLabelledBy?: string;
    removalTimerId?: number;
    swipeDirection?: SwipeDirection;
    swipeX?: number;
    swipeY?: number;
    timerDuration?: number;
    timerRemaining?: number;
    timerStartedAt?: number;
    swiping?: boolean;
    timeoutId?: number;
};
type SwipeDirection = "down" | "left" | "right" | "up";
declare global {
    interface Window {
        __starwindRuntime__?: {
            toast?: ToastManager | null;
            [key: string]: unknown;
        };
    }
}
declare function createToastManager(viewport: HTMLElement, options?: ToastManagerOptions): ToastManager;
declare function getToastManager(): ToastManager | null;
declare const createToast: ToastApi;

export { type ToastAction, type ToastApi, type ToastManager, type ToastManagerOptions, type ToastOptions, type ToastPosition, type ToastPromiseOptions, type ToastPromiseStateOption, type ToastPromiseStateValue, type ToastState, type ToastVariant, createToastManager, getToastManager, createToast as toast };
